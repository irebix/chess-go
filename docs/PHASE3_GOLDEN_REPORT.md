# Phase 3 Golden 43 真机验收记录

> 本文保留 2026-07-11/12 的历史验收结果；当前生成流程已改为只输出 PSD，不再生成同名 Manifest 与 CSV。

日期：2026-07-11，复核更新于 2026-07-12  
环境：Windows，Adobe Photoshop 2024 v25.4.0，UXP Developer Tool 调试加载

## 输入

- 工作簿：`M图标月度安排2.xlsx`
- 工作表：`越南第三十九至四十一章`
- Golden 清单：`fixtures/vietnam-02-golden.json`
- 项目数：43
- 输出目录：`artifacts/golden43-20260711-1615`

## 已确认结果

- PSD 文档：2380 × 1140 px，300 ppi；
- 顶层画板：43；
- 仅含一个子图层的画板：43；
- 智能对象子图层：43；
- Manifest items：43；
- CSV 数据行：43；
- CSV 唯一 assetCode：43；
- PSD 大小：5,921,640 bytes；
- 实际输出：
  - `M图标月度安排2_越南第三十九至四十一章_01.psd`
  - `M图标月度安排2_越南第三十九至四十一章_01.manifest.json`
  - `M图标月度安排2_越南第三十九至四十一章_01.report.csv`

## 输出命名与冲突保护

- 生成面板新增可编辑“输出名称”；输入状态与文件预览已验证，`CustomName_Test` 会预览为 `CustomName_Test_01.psd`；
- PSD、Manifest、CSV 会在生成前一次性检查同名冲突；
- 对上述已存在目录重试时，插件在创建新文档前停止，并提示修改输出名称或选择其他文件夹；
- 不再把 UXP 原始 `A File with given name already exists` 作为唯一用户提示，也不会自动覆盖已有文件。

## 2026-07-12 安全边界复核

- 旧版 Golden 43 在 Photoshop COM 的整数像素包络测量中，最大高度为 135 px，暴露出亚像素边界取整问题；
- contain 计算加入 0.5 px 安全包络后，重新完整生成 `Phase3_Golden43_Safe_20260712_01.psd`；
- 输出目录：`artifacts/phase3-safe-20260712`；
- PSD 关闭后重新打开成功，无修复提示；
- 顶层画板 43，单子图层画板 43，智能对象 43；
- 43 个智能对象的最大整数像素包络为 146 × 134 px，无越界项；
- 最大中心误差：X 0.5 px，Y 0.5 px；
- Manifest 43 项，CSV 43 行且 assetCode 唯一 43；
- PSD 大小：5,906,609 bytes。

## 取消复核

- 在 Golden 43 批量生成进行到 7/43 时按 Esc；
- UI 明确显示“已取消生成，完成 7/43。”；
- 后续项目停止创建，未完成的新文档自动关闭；
- Photoshop 文档数回到 0；
- 输出目录中未残留同名前缀的 PSD、Manifest 或 CSV；
- 清理阶段已忽略“文件已不存在”这一正常竞态，只保留真实清理失败警告。

## 2048 空白智能对象复核

- 每个画板新增一个名为 `2048x2048_空白智能对象` 的嵌入式智能对象，原图标智能对象保留；
- 智能对象源使用分层 PSB，内部画布为 2048 × 2048 px、300 ppi，包含一个透明的“在此编辑”图层；
- 主 PSD 中通过智能对象四角变换矩阵缩放到 148 × 148 px，实测 transform 为 `[0,0,148,0,148,148,0,148]`；
- 真机测试文件：`Phase3_EditableSO_Fix_20260712_01.psd`；
- 双击空白智能对象后成功打开嵌入 PSB，内部状态栏显示 2048 × 2048 px；
- 将内部图层改名为“保存测试”后执行保存，无错误提示，主 PSD 正常变为待保存状态；
- 主 PSD 再次保存成功；临时源 PSB 已被插件清理，证明后续编辑不依赖外部源文件。

## 尚待完成

- 原活动文档图层、尺寸和 dirty 状态隔离复核；
- Photoshop 2022 与 macOS smoke test。
