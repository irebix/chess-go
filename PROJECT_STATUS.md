# 棋子go｜项目状态与会话交接

更新时间：2026-07-23
当前发布版本：`0.8.3`

## 一句话摘要

“棋子go”是一个 Photoshop 2023 24.2+ 的本地 UXP 面板：读取腾讯文档导出的 `.xlsx`，按 A 列棋子链分组和图片候选生成归档 PSD；“AI初稿”支持 Flux、GPT Image 2，以及先裁切 GPT 整链、再把每张单件裁切图以 `0.2` 参考权重逐图交给 Holopix 细化的 G+F 工作流，生成结果可筛选并回填画板智能对象；面板也可执行单图 AI编辑、AI勾线，以及把所选图层组中的图片图层批量提交到 V3 动态拼接工作流并按原位置回插的 AI细化。

## 仓库与发布

| 用途 | 本地路径 | 分支 | 当前基线 |
| --- | --- | --- | --- |
| 源码、测试、文档 | `D:\Scripts\UXP\PsdArchive` | `main` | `0.8.3` |
| 同事安装用运行包 | `D:\Scripts\UXP\ChessGo-Release` | `release` | `0.8.3` |

远端公开仓库：`https://github.com/irebix/chess-go.git`。`main` 与 `release` 是同一远端的独立分支，不是嵌套目录；本地使用两个并列工作目录维护。发布分支不包含开发文档与源码。

## 当前发布快照

- `manifest.json` 与 `package.json` 均为 `0.8.3`。
- `main` 保存完整源码、测试和交接文档；`release` 只保存安装器及十二个运行文件，不直接编辑构建产物。
- `ChessGo-Release` 已同步 `0.8.3` 完整运行包和 revision 3 安装器；新增 `StandardGridBackground.png` 后共十二个运行文件，均与源码仓库 `dist` SHA-256 一致。
- `0.8.3` 已完成 66 个测试文件、314 项测试、TypeScript strict 与生产构建验证；Webpack 仅有既有 bundle-size 提示。Photoshop 最终交互验收继续由用户执行。

## `0.8.3` 发布内容

- 在“生成 PSD”正下方增加常驻单击入口“生成网格画布”，不依赖 Excel、当前文档或标准网格初始化入口。用户只需选择保存位置，插件便创建 `1780 × 1188 px`、300 ppi、RGB、非画板 PSD。
- 将用户提供的 `D:\Downloads\bg.png` 原样随插件发布为 `StandardGridBackground.png`（`1780 × 1188 px`，SHA-256 `80F9811E7F3FCAADCB3427DEF263C249BC8FBE81D2FE0EAC4B3D885617266698`）；创建时作为名为 `__BACKGROUND__` 的嵌入式智能对象精确铺满画布，不参与 96 格占用计算。
- 新文档复用现有 v1 隐藏数据存储，创建并折叠“棋子go｜标准网格画布数据”组与配置文本层，保存前同时校验背景、元数据和 `STANDARD_GRID` 识别结果；生成后可立即用于 AI初稿整排/单个回填。
- 自动化验证为 66 个测试文件、314 项测试、TypeScript strict 与生产构建全部通过；发布清单覆盖十二个运行文件并与 `dist` 的字节数、SHA-256 一致。

## `0.8.2` 发布内容

- 在 Holopix 官网实际上传整链参考图，将界面参考权重改为 `0.20` 并只提交一次生成；结果成功保持参考图的对象数量与 3×4 布局。结合官网当前前端行为、既有 HAR 与本地客户端源码，原始多模型生成仍走 `wss://holopix.cn/pino_service_api/websocket/{clientId}` 的 `module=1` 加密帧；参考图上传前置调用 OSS token 与 `localUpload`，生成 payload 使用 `layoutRefer`、`layoutReferId`、`layoutReferMode`、`sketchType`，并以顶层 `layoutReferWeight: 0.2` 表示本次权重。
- 本机 `ComfyUI-Holopix` 的 `HolopixGenerate` 新增兼容旧工作流的可选 `reference_weight`，默认 `0.2`、范围 `0–1`。客户端仅在真实参考图存在时显式写入 `layoutReferWeight`，把权重加入请求去重材料，并返回不含凭据、URL 与唯一 ID 的安全请求元数据；7 项离线测试与 `py_compile` 通过，重启后 `/object_info` 已确认新 schema。
- 新增 `GPlusF.json` 与语义适配器：`HolopixGenerateV3` 先输出 GPT 整链并保存 raw checkpoint，`AutoObjectSheetCrop` 随后按阅读顺序裁成单件批次；`AssertImageBatchCount` 同时检查最终批次数量和 `max_objects` 截断前的检测数量，任何不一致都会在 Holopix 上传与生成前失败。适配器按物品数动态创建 `ImageFromBatch → HolopixUploadReference → HolopixGenerate` 单件分支，每张真实裁切图固定 `0.2` 参考权重、`1:1`、`batch=1` 与独立 nonce，并以数据依赖严格串行；结果按原顺序合批后再进行 BiRefNet 抠图、写入 Alpha 和稳定 `assetCode` 保存。
- “AI初稿”增加 `G+F · GPT 整图裁切 + Holopix 逐图 0.2 细化` 版本；运行时加载 `GPlusF.json`、`Holopix.json` 与 `ImageRefinerStyle.png`，先把内置风格图覆盖上传到 Comfy input，再提交一次 GPT sheet 与 N 次逐图 Holopix。界面显示 `35 + 3 × N` 积分估算，并为每张串行参考图上传单独预留等待时间；候选状态、历史恢复、待确认保护、删除记录和标准网格回填均按 `g-plus-f` 与 Flux / GPT Image 2 隔离。
- AI细化的 V3 整张返回图在按槽位切回前新增 `Image Filter Adjustments`：固定以 `saturation=0.95` 表示饱和度 `-5%`，以 `brightness=0.01` 表示亮度 `+1%`，其余对比度、锐度、模糊和增强项保持中性值。工作流适配器和 Comfy 节点健康检查同步要求该节点，并在每次提交前重写参数与 `HolopixGenerateV3 → 整图调色 → DynamicObjectSheetUnpack` 连线，避免模板被误改后绕过调色。
- 本地 Comfy 端到端只提交一次两物品 G+F 冒烟任务，预计 `35 + 3 × 2 = 41` 点且没有自动重试；总耗时约 148 秒。历史核验为 `GPT sheet → AutoObjectSheetCrop → 数量与截断前检测 guard → 两个独立 ImageFromBatch → 两次独立参考上传 → 两次串行 HolopixGenerate`，两个生成节点均为 `reference_weight=0.2`、`1:1`、`batch=1` 和独立 nonce，最后按原顺序合批。输出包含 1 张 `2048×1536 RGB` raw sheet 与 2 张 `1024×1024 RGBA` 候选，Alpha 范围均为 `0–255`。
- 自动化验证为 65 个测试文件、311 项测试、TypeScript strict 与生产构建全部通过；`comfy validate` 对静态与运行时注入后的 G+F API 工作流均为 0 错误、0 警告。

## `0.8.1` 发布内容

- 根据其他环境安装后的实际目录 ZIP，确认弹窗显示 `0.8.0` 安装成功时，目标目录只有 `manifest.json`、`Holopix.json`、`GptImage2.json`、`index.html`、`main.js`、许可证和样式七项；缺少 `ImageEditor.json`、`ImageRefiner.json` 与 `ImageRefinerStyle.png`，因此 AI编辑和 AI细化会在读取插件资源时立即阻断。
- 根因是安装器在 `0.5.4` 后先后扩充运行文件清单，却一直保留 `CHESSGO_INSTALLER_REVISION=1`。旧七文件安装器下载到新 `0.8.0` 发布目录后仍只复制自身认识的七项；自更新又因远端安装器同为 revision 1 而跳过，所以“安装成功”只代表旧清单成功。
- 安装器 revision 提升为 `2`，让所有 revision 1 安装器在正式安装前强制换成最新完整清单；新版同时在 revision 相同但安装器内容变化时也执行刷新，避免未来忘记提升 revision 再次冻结旧复制逻辑。新增回归测试约束 revision、十项运行文件和同 revision 内容刷新分支，插件版本提升为 `0.8.1`。
- 安装器基础设施继续升级到 revision `3`：发布脚本不再维护硬编码运行文件数组，而是递归收集 `dist` 全部文件并生成 `release-manifest.json`（相对路径、字节数、SHA-256）；发布仓库将构建产物统一标为 `binary`，保留原始字节并确保 Git 克隆与 Codeload ZIP 的哈希一致。安装器按清单递归复制、校验并原子替换目标目录。以后新增顶层资源或子目录文件只需正常进入构建输出，无需再次修改安装器。
- 自动化验证为 61 个测试文件、294 项测试、TypeScript strict 与生产构建全部通过；Webpack 仅有既有 `main.js` 与固定参考图体积建议。

## `0.8.0` 发布内容

- `Holopix.json` 模型栈精简为两级：`日韩风格等距道具模型 | HOLO-V1`（model_id `858`、strength `0.6`）置于第一层，`油光水滑图标`（model_id `6768`、strength `0.8`）置于第二层；已删除 `欧卡场景——LSY`（model_id `7088`）及其串联引用，生成节点改接第二层模型栈。`强制 1:1 方图输出` 后新增本机 `ComfyUI-RMBG` 的 `BiRefNetRMBG` 节点，固定使用 `BiRefNet_toonout` 和 `Alpha` 透明背景，`SaveImage` 改接抠图后的 IMAGE 输出；工作流适配器会在每次提交时重新强制这条链路。
- 新增唯一支持的 `1780 × 1188 px`、`12 × 8` 标准网格画布。只有无画板、尺寸匹配且含合法隐藏网格元数据的 PSD 才解析为 `STANDARD_GRID`；其他普通画布为 `UNSUPPORTED_CANVAS`，不会自动定位或写入。初始化只复用现有隐藏组/隐藏文本层持久化机制写入模板，不保存动态占用或棋子业务状态。
- 新增纯函数网格几何、边界反推候选格、顶层 `boundsNoEffects` 占用扫描、documentId 内存缓存、单格/连续批量规划和目标格局部复检。隐藏设计内容仍占位，辅助顶层名称集中配置；覆盖完整画布的网格底板/背景层按几何特征排除，避免通用 Photoshop 图层名导致 96 格全部误判占用；有效占用要求相交面积达到图层边界与单格实体面积较小者的 20%，轻微越框不再误占相邻格，明显跨格仍占多格；扫描不递归、不读取 Alpha 像素，也不挂接 move/transform 后台监听。
- 用户界面的“AI 生成”已改名为“AI初稿”。画板候选回填分支保持原样；标准网格下始终保留 `AI初稿` 入口，插件即使在网格标签页上重载，也会扫描其他已打开的 Photoshop 文档并自动恢复来源画板链。标准网格成为活动文档后，Flux 与 GPT Image 2 的继续生成、候选数量和单格重生成仍然可用，提交前按 documentId 只读验证仍处于打开状态的来源画板 PSD 及节点范围，不再错误要求来源 PSD 必须处于活动状态。标准网格下选择候选列后提供“插入整排 / 插入当前”。首次任一写入只选择 12 格全部无内容的第一条完整空行，并新建一个带稳定链标记和绑定行的顶层 `AI初稿` 图层组；整排或单个智能对象都进入该组，组和本次写入/替换的智能对象会被显式设为可见，避免继承当前隐藏图层状态。链中第 N 个棋子固定对应该行第 N 格，候选缺失时保留空格而不压缩后续位置，链超过 12 个时明确拒绝。后续入口变为“更新整排 / 更新当前”，从原组找回绑定排：已有链位替换原嵌入式智能对象内容，空链位按固定列补齐，不重复建组且不会另开第二排。绑定组所记录的整行会参与新链空行规划；写入前排除本链组后复检目标格，再检查组内其他内容，发现冲突即停止覆盖。建组、旧版顶层散层迁移和整次写入使用单一可回滚历史事务。
- AI编辑、AI勾线和当前版本已有的 AI细化在最终写入时统一检查三种文档模式、来源 documentId/layerId 与来源几何。画板和标准网格继续插在来源上方或沿用 AI编辑顶部选项；标准网格中的 AI细化会为每个来源图层选择重叠面积最大的主格，优先保持来源尺寸与中心，并在必要时平移或缩放到 `144 × 144 px` 实体格内；普通非标准画布保留就绪结果。三者不寻找空格、不创建新网格身份、不修改占用缓存；现有 ComfyUI 工作流 JSON、画板创建与画板候选回填算法未修改。
- 新增标准网格 UI、README 使用说明与 `docs/STANDARD_GRID_MANUAL_TESTS.md` 真机清单；保持 Photoshop `minVersion 24.2.0` 与 `loadEvent: use`。

## `0.7.8` 发布内容

- 将 AI细化随插件发布的固定图2参考从旧版 `物品整理_3x4_白底.png` 替换为用户提供的 `D:\Downloads\Flux2_00045_.png`。运行时资源名继续保持 `ImageRefinerStyle.png`，上传路径、ImageBatch 的图1/图2顺序及固定提示词均不改变。
- 新参考图为 `880×1184 RGB` PNG，SHA-256 为 `845CFB2161FDDBD04501D99519EB8E5668BFF8321F2F94BA5A052B1EB2D0EA6D`。回归测试新增尺寸与完整文件哈希约束，防止构建或发布时误带旧资源。
- 54 个测试文件、252 项测试、TypeScript 与生产构建全部通过；`dist`、本地 release 目录及 UXP `0.7.8` 注册目录的十个运行文件已同步，源码与 release 均未提交或推送。

## `0.7.7` 发布内容

- 按最终要求将 AI细化客户端等待上限从 15 分钟调整为明确的 600 秒。V3 节点自身参数、任务提交和取消行为不变。
- AI细化来源从“仅图层组”扩展为“单个智能对象/栅格图层或图层组”：选择单个图层时只上传和细化这一层，结果命名为“`原图层名 细化`”并插在来源图层正上方；选择图层组时仍递归收集其中的图片图层、一次批量提交并创建同级“`原组名 细化`”结果组。
- 新增与 AI编辑一致的“高级选项 → 插入为智能对象”勾选项，默认不勾选。勾选后自动回插和就绪结果回插均保留置入智能对象；未勾选时保持原有栅格化行为。
- 54 个测试文件、252 项测试、TypeScript 与生产构建全部通过；`dist`、本地 release 目录及 UXP `0.7.7` 注册目录的十个运行文件已同步，源码与 release 均未提交或推送。

## `0.7.6` 发布内容

- 将 AI细化提交 Holopix V3 后的客户端结果等待上限从 240 秒延长为 15 分钟。V3 节点自身的 `timeout_seconds`、生成参数和付费提交行为保持不变，仅延长 UXP 对既有 prompt 的 history 轮询时间。
- 本机实测上一批 12 图层任务总耗时约 367 秒，ComfyUI 最终成功返回 12 张；新上限可覆盖该耗时，避免插件先报超时后结果才完成。新增等待上限回归测试。
- 54 个测试文件、251 项测试、TypeScript 与生产构建全部通过；`dist`、本地 release 目录及 UXP `0.7.6` 注册目录的十个运行文件已同步，源码与 release 均未提交或推送。

## `0.7.5` 发布内容

- 修复切换到不显示文档 AI 功能的 PSD 后，再切回原文档时 AI编辑、AI勾线和 AI细化状态丢失的问题。三个面板现在始终保持挂载，仅依据当前文档切换可见性；隐藏期间不会销毁组件，因此折叠状态、输入内容、选项、进度与就绪结果均可保留。
- 删除三个面板随 Photoshop 文档 ID 变化的 React `key`，避免在两个可显示 AI 页签的文档之间切换时也被强制重建。新增组件生命周期回归测试，约束隐藏只能改变显示状态，不能重新引入条件挂载或文档级 `key`。
- 54 个测试文件、250 项测试、TypeScript 与生产构建全部通过；`dist`、本地 release 目录及 UXP `0.7.5` 注册目录的十个运行文件已同步，源码与 release 均未提交或推送。

## `0.7.4` 发布内容

- 将用户提供的 `物品整理_3x4_白底.png` 原样复制为随插件发布的 `ImageRefinerStyle.png`（`576×768 RGB`，SHA-256 `306B9F36A612C86A75B00749309CADAC4C857AD82C76502E161F927E4EEC6F7E`）。构建、release 同步和安装器清单均加入该资源，运行文件由九个增加为十个。
- AI细化每次运行从插件目录按二进制读取固定参考图，通过 UXP 原生 FormData 上传到本批次隔离的 `style/blob`；工作流新增 `LoadImage` 和 `ImageBatch`，明确以 `image1=Photoshop 动态拼图`、`image2=固定风格参考图` 的顺序送入 `HolopixGenerateV3.images`。V3 继续强制使用 `vip_channel: true`。
- Comfy 主提示词固定改为“使用图2这种2.5d无描边休闲游戏卡通风格，提高图1的完成度”。UXP 输入框默认保持空白并标记为“补充要求（可选）”；用户填写内容只在提交时换行追加到主提示词末尾，留空也可直接运行。
- 本机只上传固定参考图并执行 `LoadImage(图1) + LoadImage(图2) → ImageBatch → PreviewImage` 零付费前置链，任务成功且预览批次严格返回 2 张；没有调用 Holopix V3。53 个测试文件、249 项测试、TypeScript 与生产构建全部通过；`dist`、本地 release 目录及 UXP `0.7.4` 注册目录的十个运行文件已同步，源码与 release 均未提交或推送。

## `0.7.3` 发布内容

- AI细化在 `DynamicObjectSheetUnpack` 按原槽位切回后，新增与 GPT Image 2 相同的逐图透明抠图尾链：`BiRefNetRMBG（BiRefNet_toonout）→ InvertMask → JoinImageWithAlpha`。保存节点改为接收写入 Alpha 后的批次，名称仍来自切回节点，因此图层顺序、来源映射和 Photoshop 回插位置逻辑不变。
- 运行时工作流适配器与健康检查同步要求三个抠图节点，并强制重建切回、抠图、反相、Alpha 和保存之间的连线；损坏或缺少任一节点时在付费 V3 提交前报错。
- 本机只运行 `LoadImage → BiRefNetRMBG → InvertMask → JoinImageWithAlpha → PreviewImage` 零付费尾链，任务成功输出 `123×115 RGBA` PNG，Alpha 范围为 `0–255`，含 5154 个非完全不透明像素；没有调用 Holopix V3。
- 53 个测试文件、247 项测试、TypeScript 与生产构建全部通过；`dist`、本地 release 目录及 UXP `0.7.3` 注册目录的九个运行文件已同步，源码与 release 均未提交或推送。

## `0.7.2` 发布内容

- 修复 `0.7.1` 在 Photoshop UXP 内发送手工 multipart 时触发 ComfyUI `/upload/image` HTTP 500 的问题。服务端日志确认请求在 aiohttp 解析表单阶段中断，第一张图尚未落盘，工作流与 Holopix V3 均未执行。
- AI细化恢复使用 UXP 原生 `FormData` 作为可靠传输层，不再自行设置 multipart boundary。考虑到 UXP 会把文件统一命名为 `blob`，每张图改为上传到批次目录下独立的 `001`、`002` 等子目录；工作流显式传入 `001/blob`、`002/blob`，因此不会覆盖并保持原图层顺序。
- 本机 `LoadObjectImageFolder` 对显式 `file_names` 移除扩展名白名单，改由 Pillow 实际解码文件内容；自动扫描目录时仍保留原扩展名过滤。使用 UXP 之前真实上传的 `135×140 RGBA` 无扩展名 `blob`，以及通过原生 FormData 实时上传到 `001/blob` 的 `1×1 RGBA` PNG，均完成零付费读取验证。节点文件已更新，但正在运行的 ComfyUI 仍需重启后才会载入新代码。
- 新增上传返回目录隔离、嵌套文件路径和遍历防护测试；53 个测试文件、247 项测试、TypeScript 与生产构建全部通过。`dist`、本地 release 目录及 UXP `0.7.2` 注册目录的九个运行文件已同步，源码与 release 均未提交或推送。

## `0.7.1` 发布内容

- 修复 AI细化批量上传在 Photoshop UXP 中被统一命名为无扩展名 `blob` 的问题。诊断包与 Comfy history 确认 13 张图片都已完成 Photoshop 像素读取，但 UXP `FormData.append(..., filename)` 忽略第三个文件名参数，使上传响应变成 13 个 `blob`；`LoadObjectImageFolder` 因无 `.png` 扩展名立即失败，Holopix V3 尚未执行。
- AI细化上传改为直接构造标准二进制 `multipart/form-data` 请求，在 `Content-Disposition` 中明确写入 `001.png`、`002.png` 等稳定文件名；不再依赖 UXP 的 FormData 文件名行为。客户端同时校验 ComfyUI 返回文件名必须与期望值完全一致，避免再次把多层覆盖成同一个 `blob` 后继续提交工作流。
- 新增 multipart 字节结构、PNG 文件名、上传目录安全和禁止 FormData 回退测试；本机只使用 1×1 PNG 执行零付费上传链路验证，ComfyUI 实际响应保留 `001.png` 与指定子目录，不提交 Holopix 生成。

## `0.7.0` 发布内容

- 在“AI编辑”和“AI勾线”下方新增一级折叠栏“AI细化”。用户选中一个 Photoshop 图层组后，插件会递归收集其中可读取的智能对象与栅格图层，跳过文字、调整层等非图片图层；单批最多 64 个，并在面板中显示可处理和跳过数量。
- 新增运行模板 `ImageRefiner.json`，以 `D:\Downloads\动态图片拼接-V3-原样切回-1比1或3比4.json` 为基线。运行时按稳定编号把每个来源图层以保留 alpha 的 PNG 上传到独立 ComfyUI 输入目录，动态注入有序 `file_names`、提示词、数量、nonce 和输出目录；V3 节点使用新版 `vip_channel: true`，不写回旧 `confirm_cost`。
- 工作流通过 `DynamicObjectSheetPack` 自动选择 1:1 / 3:4 拼图，Holopix V3 返回后由 `DynamicObjectSheetUnpack` 按原槽位与顺序切回，`SaveNamedImageBatch` 的历史图片记录直接绑定来源图层顺序，不依赖中文名称或重复名称猜测映射。
- Photoshop 回插会在来源组正上方创建同级“`原组名 细化`”图层组，按来源图层顺序逐张置入、在最终层级中根据生成时保存的画布边界等比适配并居中，然后栅格化；任一张回插失败会尝试清理本轮新组并保留完整就绪结果，避免重新提交付费工作流。切换文档后结果仍可在来源文档中点击“插入就绪细化图片”。
- 构建、发布同步脚本和安装器运行文件清单加入 `ImageRefiner.json`；自动化验证为 52 个测试文件、245 项测试全部通过，生产构建仅有既有 bundle-size 警告。本机 `comfy-cli 1.12.0` 对新模板校验为 0 错误、0 警告；没有为验证实际提交 Holopix 付费生成。`dist`、`ChessGo-Release` 与本机 UXP `0.7.0` 注册目录的九个运行文件哈希一致；源码与发布分支当前均未提交或推送。

## `0.6.1` 发布内容

- 同步新版 ComfyUI-Holopix 节点接口：`ImageEditor.json` 的 `HolopixGenerateV2 / HolopixGenerateV3` 以及 `GptImage2.json` 的整链 `HolopixGenerateV3` 删除旧 `confirm_cost`，改为 `vip_channel: true`；现有统一 `images` 批次入口、标题和其他连线保持不变。
- 运行时适配器同步写入 `vip_channel = true` 并显式删除可能残留的 `confirm_cost`；GPT Image 2 内部不再暴露或传递已经失效的 `confirmCost` 参数。原始三模型 `HolopixGenerate` 模板、适配器和调用参数继续保留 `confirm_cost`。
- 测试同时约束静态模板和动态准备结果的新旧字段边界。本机 `comfy-cli 1.12.0` 实时 schema 确认 V2/V3 使用 `vip_channel`、原始 Holopix 使用 `confirm_cost`；Holopix 与 GPT Image 2 静态工作流验证为 0 错误，ImageEditor 仅保留两个预期的运行时上传占位图错误。

## `0.6.0` 发布内容

- AI编辑“高级选项”中的“插入为智能对象”默认改为不勾选。用户不主动开启时，生成图片完成层级定位和尺寸适配后会按现有插入流程栅格化；手动勾选后仍保留为智能对象。
- 0.5.13–0.5.19 累积完成本地 Holopix V2/V3 图片编辑、白底正方形预处理、画板内来源图层上方定位、Huatu 式图层预览，以及生成数量、提示词输入框和高级选项的界面整理；本版本作为完整 `0.6.0` 正式提交并发布。

## `0.5.19` 发布内容

- 根据 Photoshop 真机截图进一步修复“修改要求”实际黑色输入区仍保持 Spectrum 默认约 `190 px` 宽的问题。编辑器恢复 AI 生成使用的 `.ai-prompt-editor` 结构类，以确保 UXP 命中相同的 `sp-textarea` 标签规则；专用高优先级样式继续把该结构类自带的边框、背景、圆角和内边距全部清零，因此不会恢复额外外框。
- Spectrum 控件同时增加内联 `minWidth / width / maxWidth: 100%`，专用 `sp-textarea` 规则也增加 `min-width / width / max-width: 100%` 与弹性拉伸，避免 UXP 自定义元素内部退回默认宽度。回归测试同时约束同款结构、无外框覆盖和三重全宽规则。

## `0.5.18` 发布内容

- 修复 AI编辑“修改要求”移除外层卡片后输入框变窄的问题。保持无额外边框、背景和内边距，同时为编辑器容器、文本框壳和 Spectrum 输入控件补回独立的拉伸布局：`display: flex`、`align-self: stretch`、`min-width: 0` 与 `width: 100%`，输入框重新占满控制卡可用宽度。
- 面板结构回归测试新增宽度和拉伸规则约束，避免后续样式清理再次连带删除布局属性。

## `0.5.17` 发布内容

- 研究并移植本机 `D:\Program Files\adobe\Adobe Photoshop 2024\Plug-ins\Huatu` 的图层预览链路：面板展开或切换图层后防抖 `120 ms`，在 Photoshop 模态中通过 Imaging API 读取当前精确文档/图层 ID，把图层按高度 `265 px` 缩小，再调用 `imaging.encodeImageData({ base64: true })` 生成 JPEG Base64 数据 URL，直接交给 `<img>` 显示。
- 新预览不创建原始 RGBA `ImageBlob`，与 `0.5.14` 中引发 `dynamic-torqnative.dll` 闪退的路径不同；请求带文档/图层身份校验和递增请求号，切层后返回的旧预览会被丢弃。预览失败时保留图层名称和尺寸，并在框内显示“暂不支持预览”。
- “修改要求”移除 AI 生成卡片的额外 `.ai-prompt-editor` 外框、背景和内边距，只保留 Spectrum 输入框、右下角高度拖动把手及原有 `132 px` 初始高度。
- 更新面板安全回归测试：允许 Huatu 式压缩 JPEG 数据 URL 和 `<img>`，同时继续禁止重新构造原始像素 `ImageBlob`。

## `0.5.16` 发布内容

- AI编辑“生成数量”由下拉框改为与 AI 生成“候选组”一致的减号、数字、加号步进器，仍只提供 `1 / 2 / 4` 张三个既有数量，不改变提交批次语义。
- “修改要求”复用 AI 生成提示词的 Spectrum 输入框外壳、`15 px` 正常字形和右下角纵向拖动把手；初始高度提高到 `132 px`，可在 `86–360 px` 范围内调整。当前图层预览框从 `58×58 px` 扩大到 `96×96 px`。
- “插入为智能对象”和“插入位置”收入与 AI勾线一致的“高级选项”折叠框，默认收起；插入默认值和实际逻辑保持不变。
- 删除当前图层尺寸后的“点击生成后才读取并上传”文字，以及控件底部关于 `512×512` 白底、V2/V3 和 Google API 的说明文字；后台白底预处理和本地工作流行为没有改动。新增面板结构与删除文案回归约束。

## `0.5.15` 发布内容

- 修复 AI编辑“来源图层上方”在画板或普通图层组内不稳定的问题。`placeEvent` 返回新图层后，不再依赖 Photoshop 根据当前选择自行判断插入父级；插件会按保存的来源图层 ID 在完整嵌套树中重新定位来源，并显式执行 `placedLayer.move(sourceLayer, PLACEBEFORE)`，让结果进入来源图层的同一画板/父组且位于图层面板正上方。
- 生成图层先完成层级移动，再按 ID 重新获取新的 DOM 图层对象，最后根据来源图层边界执行 contain 缩放和居中。这样即使从文档根级移动进画板后坐标上下文发生变化，也不会沿用移动前的边界对象或位置。
- “文档最上方”仍保持原行为；是否栅格化继续在层级和几何定位完成后执行。新增嵌套来源插入顺序回归测试。

## `0.5.14` 发布内容

- 修复 Photoshop 25.4 展开“AI编辑”时原生闪退。Windows 事件记录确认故障模块为 `dynamic-torqnative.dll 7.3.0.0`、异常 `0xc0000005`；崩溃发生在展开后自动读取图层像素并把动态 RGBA 交给 UXP `ImageBlob` 预览的路径。
- AI编辑展开过程现在只读取当前文档/图层 ID、名称和 DOM 边界等轻量元数据，不调用 `imaging.getPixels`、不创建 `ImageBlob`、不挂载动态 `<img>`，也不保留“刷新预览”入口。只有用户点击“上传选中图层并生成”后才读取实际图层像素并上传到 ComfyUI，V2/V3 工作流、白底 512×512 预处理和结果插入行为保持不变。
- 新增面板展开安全回归测试，直接约束 AI编辑展开组件与元数据模块不得重新引入 ImageBlob、动态图片元素或 Imaging API 像素读取。
- 本机 UXP 注册更新到 `0.5.14`；Photoshop 真机需重点验证连续展开/收起 AI编辑不再闪退，再验证点击生成后仍能上传当前选中图层。

## `0.5.13` 发布内容

- “AI编辑”从空折叠栏升级为可用的本地图片编辑流程。面板会自动跟随 Photoshop 当前选中图层并读取低分辨率 RGBA 预览；每次点击生成仍重新读取当前图层的实际像素并上传，不使用缓存预览作为输入，也不提供框选上传、第二参考图或文生图入口。
- AI编辑提供 `Holopix V3 · 2K` 与 `Holopix V2 · 2K` 工作流选择、`1 / 2 / 4` 张生成数量、修改要求、来源图层上方/文档最上方插入位置，以及是否保留为智能对象。生成结果自动按来源图层范围 contain 居中插入；批量插入中途失败时只保留尚未插入的图片，切回来源文档后可点击“插入就绪图片”，不会重新提交付费生成。
- 新增运行文件 `ImageEditor.json`，以用户提供的 `D:\Downloads\image_editor.json` 为后端基线。V2/V3 两条分支都在 `LoadImage` 后接入 `ResizeAndPadImage`：Lanczos 等比适配到 `512×512` 白底，保持完整原图、不裁切，并确保远端输入为 1:1 且边长严格大于 300 px。提交时只保留当前选择的单条 V2 或 V3 分支，避免一次任务同时触发两个付费输出节点。
- AI编辑复用棋子go现有局域网 ComfyUI 地址、任务状态监视、安全图层读取和 Photoshop 模态写入路径；不读取 API Key、不加载 Huatu 的 Gemini SDK，也不调用任何 Google API。任务支持停止等待/发送 ComfyUI 取消请求；付费工作流已生成但 Photoshop 写入条件变化时会保留就绪结果。
- 安装器运行文件清单同步加入 `ImageEditor.json`。本机 UXP 注册已从 `0.5.8` 原子切换到 `C:\Users\linkdesks\AppData\Roaming\Adobe\UXP\Plugins\External\com.linkdesks.chess-archive-psd-generator_0.5.13`，旧 `0.5.8` 已移动到 `C:\Users\linkdesks\AppData\Local\ChessGo\Backups\UxpPlugins` 可恢复备份；Photoshop 在注册更新时仍运行，需要重载插件或重启 Photoshop 后才会载入新版。
- 本版本新增工作流分支隔离、白底尺寸约束和 RGBA 图层预览像素测试，并完成正式构建、本地发布目录同步、安装文件哈希核验与 UXP 注册；`main` 与 `release` 尚未提交或推送。

## `0.5.12` 发布内容

- 在“AI 生成”和“AI勾线”之间新增一级折叠栏“AI编辑”。其显示条件与 AI勾线完全一致：没有 Photoshop 文档时整栏不渲染，打开任意普通文档或 ChessGo PSD 时显示并默认收起，切换文档后重新收起；当前展开内容为空，不显示占位文案、卡片、按钮或其他控件。
- AI勾线工作流在 `LoadImage` 后新增短边尺寸检测、`<= 300 px` 比较、短边 300 px 等比 Lanczos 放大和条件分支四个节点：输入任一边不大于 300 px 时把短边放大到 300 px，另一边按原比例变化；短边大于 300 px 时直接使用原图，不缩小。缩放判定位于 BiRefNet、背景取色和四周 20 px 扩边之前；返回 Path JSON 后按工作流实际画布与原 Photoshop 图层尺寸计算独立 X/Y 还原比例，确保放大只改善工作流输入分辨率，不改变最终描边位置和尺寸。
- AI勾线次按钮由容易与主按钮混淆的“仅生成描边”改为“插入就绪描边”，并把最近 Path JSON 绑定到生成时的精确 Photoshop 文档 ID 与图层 ID。按钮在来源文档且来源图层仍存在时显示，生成出的 `Autoline` 或其他图层处于选中状态时无需手动切回来源图层；点击后按保存的图层 ID 直接读取来源图层的位置并写入当前文档。ComfyUI 已完成但任务期间活动文档或图层变化时，插件保留就绪结果，把状态显示为“描边已就绪，等待插入”而不是把生成判为失败。切到其他文档或删除来源图层时按钮隐藏，切回来源文档后恢复；若来源图层恰好在点击后失效则明确报错。插件重载后无法证明 ComfyUI 全局最近输出的来源，因此不显示该按钮。上下文检测只遍历图层 ID，不读取像素、不新增轮询、`/prompt` 或 Holopix 调用。
- AI勾线“生成描边”每次都创建独立上传子目录并注入新的 Holopix `request_nonce`。ComfyUI 即使收到与上次完全相同的图层像素，也不会因上传文件去重和固定 nonce 把节点 2–28 整链判为缓存；主按钮始终重新上传并执行图像生成链，只有“插入就绪描边”跳过 ComfyUI/Holopix。
- AI勾线 Shape 创建后的中间工作路径清理不再直接调用生成时返回对象的 `remove()`：先按本次唯一名称/路径 ID 在当前文档重新定位，路径已被 Shape 转换自动消耗时视为清理完成；仍存在时优先调用有效 DOM `remove()`，Photoshop 25.4 未暴露该方法时回退到按路径 ID/name 的 `batchPlay delete`，避免出现 `TypeError: remove is not a function`。
- 本版本完成正式构建、发布文件同步和 `main` / `release` 双分支推送；Photoshop 最终交互验收继续由用户执行。

## `0.5.11` 发布内容

- 插件重载后从本地输出记录恢复候选时，会自动扫描“原图可回填但缺少内存预览”的槽位，并按 PSD、工作流、节点和图片键去重，通过两个独立异步通道重建预览。历史水合优先直接请求 ComfyUI `/view` 的 RGB JPEG，不再为每张旧图提交 `LoadImage → ImageScale → PreviewImage` 的 `/prompt`，因此可与付费生成同时进行；JPEG 仍由纯 JS 解码并在内存中居中缩放为 `96×96 RGBA`，再交给 uncompressed ImageBlob，不把压缩图交给 UXP 原生 `<img>`。所有成功、失败和作用域切换分支都会释放请求锁；失败按退避最多重试三次，生成结束后清空失败计数并再次扫描，避免中途生成后漏加载。新生成批次若直读失败仍保留原本的零付费本地缩放工作流兜底。
- AI 顶部计数标签由“候选数”改为“候选组”；候选矩阵的链节点固定列宽由 `84 px` 收窄为 `60 px`，“链节点 → 参考”Flex 间距由 `8 px` 收紧为 `4 px`，参考列因此实际左移 `24 px`，表头同步对齐，“参考 → 候选”的实际距离保持不变。
- “候选组”改为当前链下一轮需要生成的列数：已有候选全部完成后，插件自动在每个物品右侧补出对应数量的“待生成”槽，不再要求先点击独立的“追加 N 组候选”步骤。把候选组从 2 改成 3 时，尚未提交的尾部待生成槽同步调整为 3 个；生成中的批次不变，结束后再按最新数量补出下一轮。显式尾槽带会话内保留标记，不会被基础候选数协调器裁掉；自动补出或调整后矩阵横向滚动定位到最右侧。只有用户点击生成按钮才提交 ComfyUI；失败和结果待确认仍优先处理，不会自动新增下一轮。
- 候选矩阵每行最小高度由 `92 px` 收紧到 `78 px`，上下内边距由 `7 px` 收紧到 `3 px`；`64×64 px` 参考图与候选图及横向间距保持不变，图片外的纵向留白约压缩 `50%`。
- AI 生成统一为顶部一个主按钮，底部“重新生成选中物品 / 重新生成选中链”入口删除。主按钮优先使用矩阵现有的待生成或失败槽，不会在可见待生成列后再插入一列；只有当前没有可生成槽时才新增下一组，并且在上一轮仍运行时也可继续进入安全队列。各物品明确编辑过的提示词草稿会覆盖该物品本轮任务：Flux 直接使用文本并跳过 QwenVL，GPT Image 2 将编辑后的物品描述写入同一整链轮次；未编辑物品保持原工作流。
- AI 面板首次加载时默认选择 `GPT Image 2 · 整链候选`；Flux 仍可从工作流下拉框切换，两个工作流的候选状态、提示词草稿和历史记录继续相互隔离。
- 本版本已完成正式构建、`main` / `release` 本地提交和发布文件同步；按本次用户指令未推送远端。

## `0.5.10` 发布内容

- 所有一级折叠区展开内容相对插件可用区域的左右外缩进由 `16 px` 统一收紧为 `6 px`；AI 生成区同步采用相同外缩进，卡片自身内部留白保持不变。

## `0.5.9` 发布内容

- 新增一级折叠栏“AI勾线”，位置固定在“AI 生成”下方、“生成 PSD”上方；没有当前 Photoshop 文档时整栏不渲染，打开任意普通文档或 ChessGo PSD 时都以默认收起状态出现，不依赖参考图、画板或隐藏布局元数据；切换到另一文档时重新收起。折叠标题、卡片、按钮、配色和间距复用棋子go现有 UI，不迁移 Centerline Forge 原插件的紫色独立皮肤。
- AI勾线完整整合 `D:\Scripts\UXP\centerline-forge-uxp` 当前功能：读取当前图层像素、PPM 上传、ComfyUI 队列轮询与取消、Path JSON 校验、黑色无填充 Shape 描边及最近结果复用。面板只提供图层输出；内部工作路径在 Shape 成功后自动清理，仅在转换失败时保留用于恢复。
- 内嵌工作流以来源 `workflow-template.js` 的 18 个节点为基线，新增 `LayerUtility: GetColorToneV2` 和 `LayerUtility: ExtendCanvasV2` 两个节点：使用 BiRefNet 主体蒙版提取原图背景主色，再把图像与蒙版上、下、左、右各扩展 20 px，扩展区填充该背景色；Holopix、边界蒙版、贴合预览和矢量化统一使用扩边结果。客户端接收 Path JSON 后会去除 20 px 坐标偏移并恢复原画布尺寸，确保 Photoshop Shape 仍与原图层对齐。新结果写入 `centerline_forge/centerline_pad20.*`，不会误复用旧的未扩边结果。
- AI勾线把单次 HTTP 请求超时与完整任务等待上限分离：队列/历史请求继续使用短超时，完整任务从 180 秒提高到 360 秒；循环退出前追加一次最终历史检查，避免 ComfyUI 在约 200 秒返回时被插件提前误判为“等待处理超时”。
- AI勾线不再保留独立“状态”卡片；就绪、图层尺寸、ComfyUI 阶段、完成摘要、取消和失败信息统一以 `centerline.ai` 事件写入“运行与诊断”，错误会自动展开诊断区。AI勾线内仅保留需要实时观察的任务进度。
- AI勾线移除“Photoshop 输出”标题、“保留工作路径”、“生成工作路径”和路径名称输入；生成的工作路径与 Shape 图层统一命名为 `Autoline`。“生成描边”和“仅生成描边”位于内容区最上方。描边宽度常驻显示，精细度、折角敏感度和平滑度收入“高级选项”；高级标题与箭头作为一组靠左显示，展开内容与标题共用一个外框且参数之间没有分割线。四个滑杆右侧均配套可直接输入的数值框，取消按钮位于任务进度卡右下角。
- AI 提示词编辑草稿改为按稳定 PSD、工作流、画板和 `assetCode` 分开保存在当前插件会话中；切换物品、链条或 Flux / GPT Image 2 后再返回，不再用运行时提示词覆盖用户尚未提交的修改。新候选提示词仍只在该物品没有编辑草稿时自动同步。
- PSD AI 节点发现不再要求隐藏布局元数据中的成员画板必须包含唯一 `参考图`：参考图缺失的成员仍按原链条、中文名称和行列顺序保留在候选矩阵，并记录稳定的“参考图缺失”身份；若 Excel 仍在内存且该物品明确选择了参考图，Flux 可继续使用 Excel 参考图，用户编辑提示词和 GPT Image 2 也不依赖 PSD 参考图。没有参考图和空白智能对象的成员同样保留，第一次候选回填仍按既有逻辑创建目标。
- 连续替换同一画板候选时，回填 contain 的固定目标改为画板描述符边界，不再把上一张候选已经 contain 后的可见边界当作下一次目标框；横图与竖图交替插入不会再逐次缩小。原有替换前后画板局部/文档坐标重基、稳定图层 ID、居中纠偏和溢出检查均保留。
- 上述修复已补充提示词草稿隔离、无参考图/无目标成员发现、稳定 scope key、待确认提交持久化及连续候选固定画板目标回归测试。
- 本版本已完成正式构建与发布同步；`main` 保存源码、测试和文档，`release` 仅保存安装器及七个运行文件。

## `0.5.8` 发布内容

- 候选矩阵表头右上角新增 `···` 管理入口；展开后的操作区贴紧右边框，以独立背景、左分隔线和轻阴影区别于左侧“链节点 / 参考 / 候选”表头。进入后，每张已生成候选显示圆形选择标记，可逐张单选，也可在表头“全选 / 取消全选”；“删除”显示本轮选择数量，“完成”退出且不改动候选。管理期间工作流、链条、生成、重试和回填入口禁用，点击候选只切换删除选择，不会写入 PSD。
- 仅 `ready / accepted` 且有真实图片的候选可删除；待生成、排队中、生成中、失败和结果待确认均不可选。删除后基础候选槽恢复“待生成”，超出当前候选数的尾部历史槽自动收紧；对应本地成功输出记录同步删除，并按稳定 PSD 路径、assetCode 与工作流保存本地删除标记，后续历史恢复不会把图片重新加回来。ComfyUI 原图、历史及已经回填的 Photoshop 图层都不删除。
- “候选图片进度”左侧标题在生成期间改为实时执行状态：插件安全单队列还有后续任务时追加“本地排队 N 项”；付费工作流提交后每 `800 ms` 只读查询 ComfyUI `/queue` 并显示当前任务的实际排队位次；执行时复用提交请求的同一 `client_id` 连接 ComfyUI WebSocket，显示正在执行的节点 ID、工作流节点标题及节点自身 `value/max` 进度。WebSocket 暂不可用时保留 `/queue` 兜底并明确显示“正在执行 · 等待节点状态”，不伪造节点进度；状态监视结束即关闭连接，不修改工作流、PSD 或候选预览路径。
- Photoshop 面板 `maximumSize.height` 从 `1200` 放宽到 `2000`；默认停靠高度 `620` 与浮动高度 `720` 保持不变，因此只增加用户向下拖高面板的上限，不改变首次打开尺寸。
- 恢复按钮与相关错误提示中的用户可见文案从“恢复已有候选（不生成）”精简为“恢复已有候选”；只改文字，仍然只读取历史候选且不会提交付费生成工作流。
- 插件内四处原生 HTML 下拉框（工作表、批量选图、AI 工作流、AI 物品链）统一迁移为 Spectrum UXP `sp-dropdown / sp-menu / sp-menu-item`，通过共享 `SpectrumSelect` 使用原生 `change` 事件与 `selectedIndex` 桥接 React 状态；动态选项、禁用状态和业务语义保持不变。控件保留 Photoshop 宿主默认 Spectrum 底框与弹出层，不再使用会破坏布局的原生 `<select>` 或自定义改色外壳。
- AI 顶部控制区改为工作流、物品链和候选数的左标签右控件三行布局；工作流明确显示“单物品候选 / 整链候选”，物品链选项同时显示链名称与物品数量。生成按钮、候选进度、矩阵、提示词框及重新生成按钮之间增加轻量纵向分隔，保持窄面板中的紧凑层级，生成与恢复逻辑不变。
- 候选矩阵下方的提示词编辑器迁移为 Spectrum UXP `sp-textarea`，通过原生 `input` 事件同步 React 状态；保留既有占位文字、`86–360 px` 高度范围和输入框内右下角纵向拖动把手。输入文字使用正常字形与 `15 px` 字号，控件底色、边框和滚动行为继续由宿主 Spectrum 外观管理。

## `0.5.7` 发布内容

- 收紧 AI 面板与相邻折叠区的垂直间距，消除全部折叠时的空白条；常态隐藏“恢复已有候选（不生成）”，仅在存在待确认结果或正在恢复时显示，减少无效控件占位。
- 候选进度改为不受 UXP 原生主题覆盖的自绘进度条，使用中性中灰轨道和低饱和绿色进度；进度信息并入“每个物品生成”卡片，工作流与棋子链列表继续保持独立，并微调文字、进度条及棋子链列表的间距。

## `0.5.6` 发布内容

- ImageBlob 强制模式新增一次性分阶段诊断并接入现有 `holopix.ai` 日志/诊断导出：记录 `stage`（validation、constructor-availability、constructor、object-url、image-element、ready）、预览尺寸/字节数、`runtime/window/globalThis.ImageBlob` 类型、`URL.createObjectURL`、UXP/插件/Photoshop 运行版本和 user agent。Canvas 仍保持删除状态；本改动只增强证据，不改变渲染策略。

## `0.5.5` 发布内容

- AI 候选预览已强制只走 UXP `ImageBlob({ type: "image/uncompressed" })`；Canvas 兜底、量化绘制函数、重绘调度、样式和对应测试均已删除。ImageBlob 创建或 Object URL 显示失败时直接显示“ImageBlob 失败”并记录错误，不再降级为 Canvas。

## `0.5.4` 发布内容

- AI 面板顶部新增唯一的版本选择列表：`Flux` / `GPT Image 2`；其余候选矩阵、候选数、历史恢复、提示词编辑、点击回填和底部横向滚动布局保持不变。默认仍为 `Flux`，两种模式的候选状态和本地待确认记录相互隔离，切换回来不会混入另一种工作流的图片。
- `Flux` 完整保留现有逐物品 `参考图 → QwenVL → HolopixGenerate` 流程。
- `GPT Image 2` 使用插件根部的 `GptImage2.json`。该模板的 `LoadImage` 自带固定风格参考图 `鸡尾酒24_2_看图王.jpg`；插件不会读取或上传 Excel/PSD 参考图，也不会改写这个图片路径。运行时只注入本轮物品描述、稳定 `assetCode` 输出名、请求 nonce、付费确认、动态画布比例、裁切数量和独立输出目录。
- GPT Image 2 的一次候选轮次会把本轮所有待生成物品组成一张整链物件表，运行 `HolopixGenerateV3 → AutoObjectSheetCrop → BiRefNetRMBG → JoinImageWithAlpha → SaveNamedImageBatch`，再把透明 PNG 按 `assetCode` 拆回各行同一候选列。`每个物品生成 N 张` 等价于整链运行 N 轮；GPT 单格生成入口禁用，“重新生成选中链”会保留底部选中物品描述的编辑语义，并为当前链所有物品追加候选后重新运行完整整链。
- 节点定位不依赖 JSON 数字 ID，而使用 `GPT Image 2｜…` 语义标题并校验节点类型与关键连线。插件已经知道物品顺序和 `assetCode`，因此 GPT 模板不再运行 QwenVL OCR 标签；生成图也明确禁止文字，裁切节点直接接收插件注入的稳定名称数组，避免 OCR 错字导致候选回填到错误行。
- GPT Image 2 每次付费整链提交仍先写本地待确认记录；同一 `prompt_id` 会为每个 PSD 节点保存独立映射。历史恢复可从共享整链任务中只取回对应 `assetCode` 的输出，局部拆分失败不会丢弃已经返回的其他物品。
- 候选回填已改为供应方无关的“AI 候选”命名；Flux 与 GPT Image 2 都复用同一个稳定 PSD 身份校验、可回滚 Photoshop 历史和 contain/居中几何链。
- 构建与发布清单新增第七个运行文件 `GptImage2.json`；`publish-release.ps1`、源码安装器、Webpack 复制规则和 `ChessGo-Release` 已同步。

## 当前可用功能

- 导入本地 `.xlsx`，记住最近工作簿并可重新打开。
- 自动选择第一个工作表；切换工作表时自动解析棋子链。
- 按 A 列连续分组选择范围，默认不全选，工具栏提供“全选 / 清空全选”。
- 多排图片候选全部展开，支持按排位批量选图；单个棋子某一排缺图时不强制匹配。
- 缩略图可见区域延迟加载，限制实时缩略图与缓存数量，避免大表卡顿和闪烁。
- 非标准或缺失 `assetCode`、无关联图片都保留提醒但不阻断生成；无关联图片的项目仍创建画板与空白智能对象，只是不放参考图。
- 只生成 PSD。首次保存由 Photoshop 弹窗命名；多卷自动续号，并在生成前检查后续同名冲突。
- 每个棋子画板包含参考图和嵌入式空白智能对象；空白智能对象边长可配置，默认 `1024 px`，画板内显示为 `148×148 px`。
- 画板间距默认 `50 px`；智能对象边长和画板间距会恢复用户上次的合法输入。
- 生成的 PSD 可隐藏/恢复参考图，并记录切换前的图层可见状态。
- 可显示/隐藏分组框；分组框由底层分组画板实现。
- 可统一修改原生画板背景颜色，也可隐藏为透明并恢复设置颜色。
- 布局元数据存放在默认折叠的隐藏数据组，供分组框、底板控制和 PSD 独立恢复使用；成员记录在原有画板 ID、行、列之外兼容保存可选物品中文名称，不新增可见图层。旧版三字段成员数据仍可读取。
- “AI 生成”与“当前 PSD”使用相同的自动识别条件，仅在当前文档是可识别的棋子归档 PSD 时显示。AI 面板会只读扫描当前 PSD 中画板名称、`参考图` 图层与空白智能对象，并读取既有隐藏布局元数据中的真实链条标签、成员画板 ID、行列顺序和物品中文名称；隐藏布局中的成员即使没有 `参考图` 也按原链条保留矩阵行，参考列显示 Excel 中明确选择的图片或“无图片”，不会再删除整行。缺少空白智能对象同样不影响候选生成；第一次点击该行候选时，插件会在对应画板中直接创建嵌入式智能对象、按现有 PSD 的主流尺寸命名并完成 contain/居中回填。只有同画板存在多个目标时，候选列显示“空白智能对象不唯一”并禁止该节点生成和回填。候选回填期间，同文档结构事件的扫描先由 scope gate 暂存，只要替换或创建可能已经写入，结束后就最多约 `800 ms` 进行多次严格画板归属复查。普通观察与每个付费批次提交前始终使用严格扫描；生成范围按稳定 PSD 身份、画板 assetCode、画板 ID 与参考图存在/缺失身份复核，目标智能对象仅在回填时严格复核或创建，因此自动补建目标不会清空候选状态或取消同节点后续批次。名称同时用于矩阵第一行和 QwenVL“物件名字输入”，assetCode 保留在第二行；旧 PSD 没有成员名称且 Excel 不在内存时才回退 assetCode。PSD 参考缩略图由 Photoshop Imaging API 在 modal scope 内直接编码为 JPEG data URL；提交 QwenVL 时同样只读编码为 JPEG。Photoshop 25.4 实机要求连 `imaging.getPixels` 只读调用也位于 modal scope，因此预览和 QwenVL 取图共用串行 `executeAsModal` 读取队列；固定 sRGB 配置失败时自动回退到文档/工作 RGB 配置。读取过程不写入、不修改 PSD。若对应 Excel 仍在内存中，优先复用名称等表格信息。
- Holopix 候选数可设为 `1–4`；按棋子使用安全单队列，三张候选自动拆为 Holopix 支持的 `2 + 1` 批次；拆分批次只在首批运行 QwenVL，后续批次复用首批真实提示词，保证同一行候选语义一致。每批 SaveImage 原图返回后会立即进入候选格，再异步增强安全预览；后批或预览失败不会丢弃前批已付费原图，单批只返回部分图片时也只把未返回的剩余格标为失败。
- Holopix 使用 `参考图 → LoadImage → AILab_QwenVL → 文字提示词 → HolopixGenerate`；当前物品的 `name` 注入标题为“物件名字输入”的 `PrimitiveStringMultiline`，再经 `StringFormat` 提供给 QwenVL。`HolopixGenerate` 明确删除 `reference` 输入，因此不是图生图。
- 标题为“提示词结果”的 `PreviewAny` 记录 QwenVL 的运行时文字输出；新生成和新历史候选会携带真实提示词，候选矩阵下方显示当前物品已选候选或首个候选的实际文本，不由插件自动编写。
- 候选矩阵下方的当前物品提示词可直接编辑；编辑草稿按当前 PSD、工作流与物品独立保存到插件会话，切换物品或工作流后返回仍保留尚未提交的修改。编辑框使用宿主 Spectrum 外观并与矩阵左右对齐，右下角输入框内部的细纹把手可在 `86–360 px` 范围内只调整高度。“重新生成选中物品”会在该物品原候选右侧追加“每个物品生成”当前数量的新槽位，旧候选不覆盖。即使整条链仍在生成也可点击，新槽位立即显示排队状态并进入同一个 Holopix 安全单队列。该任务直接把用户文本交给 `HolopixGenerate.prompt`，不读取或上传参考图，并从本次提交图中移除 `LoadImage → 物件名字输入 → StringFormat → AILab_QwenVL` 执行链；“提示词结果”节点直接记录用户文本。失败槽会保留本次用户提示词或首批 QwenVL 实际提示词；单格与批量重试按提示词来源分批，不会重新绕回 QwenVL，也不会重生成已经成功的前批图片。
- 生成节点强制 `aspect_ratio: 1:1`，本地 `ImageScale` 再把保存结果规范化为精确的 `1024×1024` 方图。
- Photoshop 25.4 不能稳定使用 UXP `<img>` 直接解码动态 Holopix 的压缩 PNG/JPEG；候选预览优先通过 ComfyUI `/view` 只读取得 RGB JPEG，由纯 JavaScript 解码并在内存中居中缩放为 `96×96 RGBA`，再通过 UXP `ImageBlob({ type: "image/uncompressed" })` 生成高清 `<img>` Object URL。历史预览直读不提交 `/prompt`，可与生成并行；新生成批次直读失败时才使用零计费的 `LoadImage → ImageScale → PreviewImage` 工作流兜底。两条路径都不把压缩数据交给宿主原生解码器；ImageBlob 不可用时直接报错，不再提供 Canvas 降级路径。
- Excel 参考图使用 `object-fit: contain` 放入 `1:1` 方格：长边贴住方格边缘，短边留白，始终完整显示而不裁切。
- 候选方格保持 `1:1`，只显示 `96×96` 原始 RGBA ImageBlob 高清缩略图；失败时在原槽位明确显示 ImageBlob 错误。候选行使用 `IntersectionObserver` 按可视区挂载并释放离屏 Object URL。矩阵内容本体不使用原生滚动，底部独立横向滚动条以 `scrollLeft → translateX` 同步水平位置，避免横向滚动容器吞掉顶层纵向滚轮。候选角标和可见“选用”按钮均已移除，只有已选项显示绿框。
- “恢复已有候选”仅在存在待确认结果或正在恢复时显示；它会从 ComfyUI 最近最多 1000 条历史找回当前棋子链的 `Holopix/ChessGo` 输出，不提交 Holopix 生成工作流。恢复时以最新输出的真实提示词为批次边界，只合并提示词完全一致的候选，避免把旧任务图片混入当前候选排。每个付费 `/prompt` 在请求发出前即以本地持久记录为硬前置：记录失败时请求不会发送；拿到 `prompt_id` 后继续更新。记录按规范化完整 PSD 路径身份、assetCode、画板与参考层稳定 ID 精确匹配，同名 PSD 或复制模板不会误接管；目标智能对象 ID 只保留为诊断字段，不参与恢复匹配，因此缺失目标在回填时补建后不会丢失已付费记录。未保存或 Photoshop 无法提供稳定路径的 PSD 会先要求保存，不允许付费提交。确定拒绝时删除记录；已返回原图或按 `prompt_id` 精确恢复成功时改存为 `outcome=output` 原图元数据，插件重载后直接恢复为 ready 而不是 idle，防止再次付费；安全预览可由恢复操作按同图 key 就地增强，不覆盖原图或 submission 身份。切换/关闭 PSD、面板隐藏或插件重载后，未确认记录会恢复为“结果待确认”并禁止直接重试；已取得 `prompt_id` 的提交会优先按该 ID 单独读取历史，并且只填入同一 `prompt_id` 的待确认格。按 assetCode 查找的宽泛历史只补普通空格，不会再用旧图清除本次待确认状态。HTTP `408/5xx`、响应正文中断或缺少 `prompt_id` 都按可能已受理处理；无 ID 的记录必须由用户在检查 ComfyUI 后点击“确认放弃待确认结果”才能重新生成。
- 运行时通过 UXP `getPluginFolder()` 读取当前实际加载插件目录根部的 `Holopix.json` 与 `GptImage2.json`。两个模板均在首次读取后按当前插件会话缓存，修改文件必须重载插件才生效。Flux 模板使用 QwenVL，运行时会注入参考图路径、物品名称/用户提示词、候选数量、请求 nonce、付费确认、保存前缀、`1:1` 与 `1024×1024` 方图约束，并始终删除 `HolopixGenerate.reference`。GPT Image 2 模板保留 JSON 内置风格图，只注入整链物品描述和输出映射等运行参数。
- ComfyUI API、历史、上传、候选图和安全预览统一使用局域网端点 `http://192.168.1.32:8188`，不再生成 `127.0.0.1` 地址；本机 Comfy Desktop 的该安装实例已配置追加 `--listen 0.0.0.0`，监听所有 IPv4 接口，插件和浏览器仍使用具体可访问地址而不把 `0.0.0.0` 当作客户端地址。配置需重启 ComfyUI 后生效；本机同时存在以太网和 Tailscale，Windows 防火墙应把 8188 端口限制在可信网络范围。
- AI 面板只提交当前 PSD 范围内实际勾选、并且明确选择了 Excel 参考图的节点；不会因为选择了整条分组而把组内未勾选节点或默认第一张参考图一并提交。生成任务与每个付费批次提交前都会复核当前 document、稳定文件身份、artboard 与参考层；缺少目标不阻断生成，多个目标的歧义节点仍不提交。切换 PSD 或改变节点范围后，尚未提交的旧任务会停止，不继续为已离开范围的节点付费。点击生成后直接提交 Holopix 工作流，不显示二次确认弹窗；自动化验证不提交真实生成任务。
- 候选生成或历史恢复后，直接点击候选即可立即替换对应画板的空白智能对象；同一行继续点击其他候选可在画板中换图对比。
- 选择候选后，插件先确认矩阵记录的 document、artboard 与参考层稳定 ID 仍和当前 PSD 一致。已有唯一 `数字x数字_空白智能对象` 时继续严格核对目标 ID 后替换；目标确实缺失时，将候选原图作为新的嵌入式智能对象置入原画板并命名，不先创建多余的空白 PSB；若此时目标已经出现、变成多个或画板结构变化则停止写入。下载候选期间切换文档、重命名画板、替换或移动目标同样会停止。select、replace 与 place 的 `batchPlay` 返回分别检查错误描述符；创建/替换、移入画板、缩放、平移和最终几何检查封装为单个可回滚 Photoshop 历史状态，任何失败都会用 `resumeHistory(..., false)` 撤销本次写入。替换后按稳定的画板/图层 ID 重新取对象；新建路径还会严格确认新图层实际嵌套进原画板。替换路径始终用画板描述符矩形作为 contain 的固定目标，不继承上一张候选的可见边界；测量优先使用 DOM `boundsNoEffects → scale(MIDDLECENTER) → translate(px)` 链，DOM 不可用时使用 `smartObjectMore.transform`。若 Photoshop 在操作前后把画板从局部坐标切换到文档坐标，目标框按当前画板坐标重新建立；执行后允许一次误差校正，并检查最终中心偏差与四边溢出。每次回填会把文档/画板/图层 ID、嵌套路径、artboardRect、DOM bounds、transform 四角、`target.rebased` 及各阶段误差写入诊断日志。
- 运行日志和诊断默认收起；可以导出不包含 XLSX 与图片二进制的诊断 ZIP。
- “AI勾线”作为“AI编辑”与“生成 PSD”之间的独立一级折叠区存在；展开后可把 Photoshop 当前活动图层提交到 24 节点 Centerline Forge 工作流并输出黑色无填充 Shape 描边图层。“插入就绪描边”只使用当前插件会话中与活动文档完全匹配、并绑定精确来源图层 ID 的最近就绪结果；它直接读取该来源图层，无需来源图层当前被选中，也不上传图层或提交 ComfyUI/Holopix。内部工作路径成功转换后自动清理。长任务显示轮询进度并可从进度卡右下角取消，详细状态统一写入“运行与诊断”。生成写入前重新确认活动文档与来源图层，插入就绪结果前重新确认活动文档并按来源图层 ID 取像；所有 Photoshop 读取和写操作位于 `executeAsModal`。

## 主要代码入口

- 面板与交互：`src/app/App.tsx`
- AI 候选矩阵：`src/app/AiGenerationPanel.tsx`
- AI勾线面板与工作流：`src/app/AiOutlinePanel.tsx`、`src/centerline/`
- Holopix 工作流适配与 ComfyUI 客户端：`src/ai/holopixWorkflow.ts`、`src/ai/holopixClient.ts`
- GPT Image 2 整链适配、客户端与恢复：`src/ai/gptImage2Workflow.ts`、`src/ai/gptImage2Client.ts`、`src/ai/gptImage2Recovery.ts`
- 候选回填：`src/photoshop/aiCandidateBackfill.ts`
- 样式：`src/styles.css`
- 批量 PSD 生成：`src/photoshop/batchGenerator.ts`
- 参考图与当前 PSD 控制：`src/photoshop/referenceViewController.ts`
- 分组框及布局元数据：`src/photoshop/groupArtboardOverlay.ts`、`src/domain/groupLayoutMetadata.ts`
- 原生画板底板：`src/photoshop/artboardBackgroundController.ts`
- XLSX 解析：`src/infrastructure/xlsx/`
- A 列分组和映射：`src/domain/sheetGroups.ts`、`src/domain/mapper.ts`
- 生成参数记忆：`src/domain/generationSettings.ts`、`src/services/GenerationSettingsService.ts`
- Windows 安装器源：`installer/install.cmd`
- 发布同步：`scripts/publish-release.ps1`

## 最近完成

- `0.5.10`：所有一级折叠区展开内容相对插件可用区域的左右外缩进由 `16 px` 统一收紧为 `6 px`；AI 生成区同步采用相同外缩进，卡片自身内部留白保持不变。
- `0.5.9`：在“AI 生成”下方加入一级折叠栏“AI勾线”，将 Centerline Forge 的当前图层读取、20 节点 ComfyUI 工作流、进度/取消、Shape 图层写入、最近结果复用及四个调节项迁入 React/TypeScript 主插件；工作流在 ComfyUI 内部按背景主色四周扩展 20 px，并在返回时补偿路径坐标。视觉复用棋子go既有控件体系，独立状态卡片合并到“运行与诊断”，生成动作置顶，高级参数合并折叠且支持滑杆/数字双向输入。另包含 AI 提示词草稿隔离、缺少参考图时保留矩阵成员、连续候选回填使用固定画板边界等修复，运行文件清单保持七项不变。
- `0.2.4`：安装器先比较 GitHub 远端版本；相同版本跳过 Git，有更新时优先 Git，Git 通道失败后回退到 Codeload 精确提交压缩包。
- `0.2.5`：在画板间距上方增加“智能对象边长”，默认 `1024 px`；实际创建对应尺寸的正方形 PSB，图层名带尺寸，画板内仍为 `148×148 px`。
- `0.2.6`：画板间距默认从 `100` 改为 `50 px`；用 `localStorage` 持久化智能对象边长和画板间距，只记录两项都合法时的最新值。
- `0.2.7`：无关联图片改为非阻断提醒；继续生成不含参考图的画板与空白智能对象，并保留分组框和底板控制。
- `0.3.0`：接入 Excel 参考图 → Holopix → AI 候选矩阵 → 画板智能对象回填流程；增加 UXP 兼容面板、付费确认、批量并发、单格重试和可直接编辑的 `Holopix.json`。
- `0.3.1`：根据诊断包修复 UXP `Manifest entry not found` 网络权限；AI 栏跟随当前 PSD 自动显隐，缩略图改为 `1:1`，提示词只来自工作流，并取消生成前弹窗。
- `0.3.2`：根据 Photoshop 原生崩溃记录和 ComfyUI 输出时间线，移除候选区对本机 HTTP 原图的直接渲染，改用顺序读取的受限 JPEG data URL；生成并发降为单队列并补充阶段日志。
- `0.3.3`：二次崩溃证明压缩后的 JPEG 仍为 `1024×992`，解码时继续触发 Photoshop 25.4 原生访问冲突；改由 ComfyUI 先生成真正的 `96×96` 临时 PNG，插件校验 PNG 头尺寸后才显示，并在棋子任务间增加解码缓冲时间。
- `0.3.4`：真实 `96×96` PNG 在候选返回后仍触发相同 Photoshop 原生偏移崩溃，因此彻底取消 UXP 内的 Holopix 图片解码；候选方格改为“系统浏览器查看”和“选用回填”，并可从 ComfyUI 历史恢复已有候选而不重新生成。
- `0.3.5`：新增零计费安全预览工作流，把既有输出经 ComfyUI `LoadImage → ImageScale → PreviewImage` 规范化为 `96×96` RGB JPEG；插件使用 `jpeg-js` 纯 JavaScript 解码，再量化为 `48×48` Canvas 基础色块绘制，不再调用 UXP `<img>` 解码器。候选矩阵和已选链均恢复面板内预览，失败时继续保留外看/选用。
- `0.3.6`：Holopix 改为真正的提示词-only 工作流，不再上传 Excel 参考图；提示词支持节点占位符并在面板顶部显示解析后的实际文本。生成强制 `1:1` 并规范化为 `1024×1024`；候选卡移除全部角标和选用按钮，点击图片选择，只有已选项显示绿框；回填后按原画板框重新 contain 和居中。
- `0.3.7`：纠正 `0.3.6` 对“不图生图”的误解，恢复 Excel 参考图上传和 Holopix 图生文，但彻底断开生成节点的 `reference`，形成图生文再文生图；记录并显示图生文节点真实输出。候选缩略图外层从 UXP 原生按钮恢复为已验证的普通容器，保留整图点击和仅已选绿框，修复 Photoshop 25.4 中按钮内 Canvas 不显示。
- `0.3.8`：修复 `0.3.7` 扁平化候选 DOM 后多 Canvas 只显示少量缩略图的回归；恢复 v0.3.5 已验证的 `Canvas + 原生交互层` 合成结构，但交互层完全透明，因此界面仍无角标和选用按钮。Excel 参考图改为长边贴边的 `contain` 显示，不再按短边铺满裁切。
- `0.3.9`：候选 Canvas 与方格统一为 `64×64` 满格显示；原生交互层改为 UXP 可稳定命中的近透明表面，并增加容器点击回退。历史恢复按最新真实提示词隔离批次，不再把旧任务的第二张图片拼进当前行；三候选 `2 + 1` 拆批时后续生成复用首批图生文提示词。
- `0.4.0`：候选矩阵改为每行单 Canvas、可视区挂载、滚动后强制重绘和单层外滚动，采样绘制量最多下降四分之三，降低 Photoshop UXP 的 surface 与合成压力。回填改为唯一目标校验、替换后重新取 Layer、同源 DOM/transform 测量、二次中心校正和最终溢出检查，并向诊断包记录完整几何链。
- `0.4.1`：根据实机诊断中 `c_cleaning1` 替换前 `0…148`、替换后 `1732…1880` 的坐标系跳变，把回填目标按替换前后画板位移重新基准，并改为用稳定画板/图层 ID 取回对象。候选 Canvas 改为命令式定点重绘、`16×16` 采样和纵向同色合并，避免滚动后整面板重渲染，并把单候选最坏绘制次数从 1024 降至 256。
- `0.5.0`：候选预览改用未经 PNG/JPEG 压缩的 UXP ImageBlob 原始 RGBA 路径并保留 Canvas 兜底；AI 范围可由当前 PSD 的参考图、唯一回填目标和隐藏布局元数据只读重建，恢复原链条、中文物品名和成员顺序。PSD 参考取像按 Photoshop 25.4 要求在串行 `executeAsModal` 中执行，但不写入文档。工作流切换到 QwenVL，物品名称进入“物件名字输入”，“提示词结果”保存真实提示词；提示词编辑框支持修改、纵向缩放，并可跳过 QwenVL 追加生成选中物品而不覆盖旧候选。候选矩阵使用可视区 ImageBlob、独立水平滚动条和持续扩展的候选槽位；生成与回填忙碌状态解耦，后续任务排队时已完成候选可立即回填。ComfyUI 统一改用局域网端点 `192.168.1.32:8188`，服务端配置为 `--listen 0.0.0.0`。
- `0.5.1`：修复 `0.5.0` 将 PSD 参考缩略图强制改成 uncompressed ImageBlob 后，在 Photoshop 25.4 当前 UXP 上下文未暴露 `ImageBlob` 时全部显示失败的回归。PSD 参考预览恢复为 Imaging API 在 modal scope 内直接编码的 JPEG data URL；动态 Holopix 候选仍保持 JS JPEG 解码为 RGBA 后优先使用 uncompressed ImageBlob、Canvas 兜底，不退回 UXP `<img>` 原生解码压缩候选图。
- `0.5.2`：确认 `c_cleaning1` 整行消失的直接条件是对应成员画板没有唯一 `数字x数字_空白智能对象`；节点发现现改为以唯一参考图保留矩阵行。目标缺失不再影响生成，首次候选回填会在同一个可回滚 modal 历史事务中创建嵌入式智能对象、移入原画板并完成缩放居中；目标重复仍以“空白智能对象不唯一”阻断。另修复切文档/同名画板可能写错目标、`batchPlay` 假成功及几何失败后未回滚；保留自定义/首批 QwenVL 提示词用于重试，保留 `2+1` 与单批部分结果；原图先于安全预览落槽，即使提示词捕获节点缺失、捕获文本不一致或后续执行报错也不丢弃已付费输出；切换 PSD 后停止旧队列未提交批次；付费提交以前置持久化、稳定 PSD 文件身份和未保存文档阻断消除重复收费窗口，未知任务按 `prompt_id` 精确恢复，成功输出继续作为 durable output ledger 水合为 ready，不会在重载后退回 idle。
- `0.5.3`：候选矩阵不再用 CSS `translateX` 移动画面。Photoshop UXP 实机证明 `overflow: hidden` 视口的 `scrollLeft` 不产生可见滚动，而让视口自身使用原生 `overflow-x: auto` 又会先吞掉候选区域的纵向滚轮；最终方案保留独立底部横向滚动条和非滚动视口，使用负 `margin-left` 进行真实布局位移，使视觉位置与按钮命中一起移动，同时让候选区域的纵向滚轮继续由最外层面板处理。水平偏移按内容与视口宽度夹紧，动态追加候选后不会停在无效区域。
- `0.5.4`：AI 生成模块新增顶部 `Flux / GPT Image 2` 版本选择，Flux 保持原逐物品流程；GPT Image 2 使用 `GptImage2.json` 内置风格参考，一次生成完整物件链并按稳定 `assetCode` 裁切回填。节点按语义标题定位，候选状态与待确认记录按工作流隔离；GPT 单格生成入口禁用，“重新生成选中链”会为整链所有物品追加候选并重新运行完整链。候选回填命名同时改为供应方无关的“AI 候选”。
- `0.5.5`：动态 AI 候选预览强制只使用 UXP uncompressed ImageBlob；删除 Canvas 兜底组件、量化绘制函数、滚动/尺寸重绘调度、相关样式与测试。ImageBlob 创建或 Object URL 显示失败时直接在槽位提示并记录错误，不再静默降级。
- `0.5.6`：ImageBlob 强制模式新增完整分阶段运行诊断，成功和失败均写入现有 `holopix.ai` 日志与诊断导出；记录 API 暴露位置、UXP/插件/Photoshop 版本、预览像素信息以及构造器、Object URL、HTMLImageElement 等具体阶段。Canvas 继续保持删除。
- `0.5.7`：收紧折叠区间距并按状态隐藏恢复按钮；候选进度改为 UXP 不可覆盖的自绘中灰/绿色进度条并合入生成卡片，保持两个选择列表独立，微调进度与棋子链列表间距。
- `0.5.8`：候选矩阵新增 `···` 管理入口、单选/全选和仅删除本地候选的持久化屏蔽；生成进度实时显示插件与 ComfyUI 排队位置、节点标题和节点自身进度；面板最大高度放宽到 `2000 px`，恢复按钮文案精简为“恢复已有候选”。
- `0.5.11`：历史候选预览改为双通道只读 `/view` 异步水合；候选组自动提供下一轮待生成槽并统一顶部生成入口，编辑提示词可直接用于现有槽；默认工作流改为 GPT Image 2，矩阵列距与纵向留白进一步收紧。
- `0.5.12`：新增空白“AI编辑”一级栏；AI勾线增加小图短边 300 px 条件放大、强制新任务 nonce、来源图层绑定、“插入就绪描边”和 Photoshop 25.4 工作路径清理兼容。

## 验证状态

- 最近一次 `pnpm verify`：通过。
- TypeScript strict：通过。
- Vitest：`46` 个测试文件、`229/229` 测试通过。
- Webpack production build：通过；`GptImage2.json` 已进入 `dist`，仅有 `main.js` 体积建议警告（约 540 KiB），不是构建失败。
- 当前局域网 ComfyUI 已完成只读直读预览核对：从历史 output 请求 `/view?preview=jpeg;82&channel=rgb` 返回 HTTP `200`、`image/jpeg`、有效 JPEG 签名，实际约 `30 KiB / 512×512`，并由项目同款 `jpeg-js` 参数成功解码为 RGBA；核对过程未调用 `/prompt`。
- AI勾线已通过局域网 ComfyUI 只读核对：当前工作流引用节点 `19/19` 可用，短边读取、数值比较、条件分支、等比缩放、背景取色与画布扩展节点输入/输出类型匹配，`BiRefNet_toonout` 模型存在；没有提交 `/prompt`、Holopix 付费生成或 Photoshop 自动化交互。新的小图缩放分支需首次实机生成后验证。
- `ChessGo-Release/release` 已同步正式 `0.5.12`，七个运行文件与源码 `dist` 哈希一致并已提交、推送。
- 已通过局域网 ComfyUI `object_info` 只读核对 `LoadImage`、`PrimitiveStringMultiline`、`HolopixGenerateV3`、`AutoObjectSheetCrop`、`BiRefNetRMBG`、`InvertMask`、`JoinImageWithAlpha`、`SaveNamedImageBatch` 均存在且输入/输出类型匹配；没有提交 `/prompt` 或付费生成任务。
- Photoshop 2024 实机已重启并加载 `0.3.4`：导入 1.2 MB 带图 Excel、选择 9 个参考图后，用“恢复已有候选（不生成）”找回 5 张历史候选；候选仅显示“查看 / 选用”，持续观察 30 秒 Photoshop 未闪退，且没有新增应用崩溃事件。
- Photoshop 2024 实机已通过 UXP Developer Tools 热重载 `0.3.5`：导入同一带图 Excel、选择清洁工具链后，从 ComfyUI 历史恢复并直接绘制 `18/18` 张候选；持续观察 30 秒面板保持可用、Photoshop 正常响应，新增应用崩溃事件为 `0`，日志确认未提交新生成任务。
- Photoshop 2024 已热重载 `0.3.6`：面板顶部正确显示“清洁布”的解析后实际提示词，并明确提示“不读取或上传参考图”；本机 `object_info` 确认 `HolopixGenerate.reference` 为 optional、`ImageScale` 支持当前方图参数。UI 自动化定位浮动 UXP 面板时两次误点生成按钮，共提交了 2 个“清洁布”单图任务；历史记录确认两次请求均为纯文本、`aspect_ratio=1:1`、不含 `reference`，输出 `c_cleaning1_00008_.png`、`c_cleaning1_00009_.png` 均为实际 `1024×1024`，检查结束时 ComfyUI running/pending 队列均为 `0`。未继续点击候选回填，以免改动用户当前未保存 PSD。
- `0.3.7` 已通过本机 ComfyUI 零付费节点实测：从既有输入图仅运行 `LoadImage → HolopixUploadReference → HolopixImageToPrompt → easy showAnything`，约 3 秒后历史输出真实中文提示词；该验证没有包含或执行 `HolopixGenerate`。`object_info` 同时确认 `HolopixGenerate.reference` 为 optional，发布工作流中该输入不存在。
- Photoshop 2024 已热重载 `0.3.8`：重新打开当前 PSD 与最近 Excel，选择清洁工具链后用“恢复已有候选（不生成）”恢复 `18/18` 张历史候选；上下滚动逐行确认 9 个节点、18 张 Canvas 缩略图全部可见。纵向和横向 Excel 参考图均按长边贴边完整显示，无裁切；面板状态和日志确认未提交新生成任务，也未点击候选回填。检查结束时 ComfyUI running/pending 均为 `0`，Photoshop 继续响应，最近 30 分钟新增 Photoshop 崩溃事件为 `0`。
- Photoshop 25.4 已热重载当前测试包：用户确认 ImageBlob 候选可正常加载且无闪退；清洁工具链取消勾选 `c_cleaning8`、`c_cleaning9` 后，AI 面板范围从 9 条准确变为 7 条，历史恢复也只恢复这 7 条且未提交新生成任务。点击 `c_cleaning1` 候选后已实际回填当前 PSD，最终图层边界为 `[1732,148,1880,296]`，中心误差 `[0,0]`、四边溢出均为 `0`。
- Photoshop 25.4 / UXP `uxp-7.3.0-6` 已完成一次 GPT Image 2 整链 1 轮、6 张候选的成功实测；ImageBlob 诊断为 `stage=ready`，`runtime/window/globalThis.ImageBlob` 与 `URL.createObjectURL` 均为函数，预览为 `96×96 RGBA / 36864 bytes`。直接 Photoshop 字段为 `missing`，但 user agent 明确记录 Photoshop `25.4.0`；该基线证明当前面板上下文支持 ImageBlob 构造与 Object URL 路径。

## 仍需人工验证

- 点击“生成 PSD”正下方的“生成网格画布”，确认只弹出一次 PSD 保存位置选择；生成文件应为 `1780 × 1188 px` 非画板文档，`__BACKGROUND__` 智能对象完整铺满且可见，底部“棋子go｜标准网格画布数据”组默认隐藏并折叠。保存、关闭、重开后面板应直接识别为 `12 × 8` 标准网格、显示 `0 / 96` 占用，并可让 AI初稿选择首条空行完成整排或单个回填。
- 在 Photoshop 中对一组图片执行 AI细化，确认 Comfy 历史链路为 `HolopixGenerateV3 → Image Filter Adjustments → DynamicObjectSheetUnpack`，调色节点显示 `brightness=0.01`、`saturation=0.95`，整图预览为调色后的结果；切回数量、原槽位、后续 BiRefNet 抠图和 Photoshop 回插位置均不得改变。
- 在 Photoshop 中重载 `0.8.2`，确认 AI初稿版本列表出现 `G+F · GPT 整图裁切 + Holopix 逐图 0.2 细化`。选择 2–3 个棋子运行一轮：日志应先显示内置 `ImageRefinerStyle.png` 已上传，再完成 GPT raw sheet 与裁切数量校验，然后逐张显示参考图上传和 `0.2` 权重 Holopix 细化；第 N+1 张不得在第 N 张生成完成前上传。Comfy 历史中的每个 `HolopixGenerate.reference` 都必须连接对应的单张 `HolopixUploadReference`，不能直接连接 GPT 整图或只经过图片转提示词。最终候选应按 `assetCode` 回到正确行、带透明 Alpha，并可在画板 PSD 与标准网格中沿用现有整排/单个回填；切换到 Flux 或 GPT Image 2 后候选、待确认状态与历史恢复不得串线。
- 在 Photoshop 中重载 `0.7.8`。确认 AI细化上传的图2为新版 `Flux2_00045_.png` 内容，图1仍为 Photoshop 来源拼图；输入框初始为空并显示“补充要求（可选）”，留空时仍可运行。分别选择单个智能对象、单个栅格图层，以及同时含智能对象、栅格图层、嵌套子组和文字/调整层的图层组：单图层应只上传一张并在来源正上方插入“`原图层名 细化`”；图层组应只统计并上传可读图片层，非图片层显示为跳过，并在来源组正上方创建同级“`原组名 细化`”组。分别在“插入为智能对象”未勾选和勾选时验证输出为栅格图层和智能对象；每张切回图片都应经过 BiRefNet 批量抠图并带透明 Alpha、保持来源顺序和画布位置。任务中途切换文档时结果不得写入错误文档，切回来源文档后“插入就绪细化图片”应可直接回插且不会重新提交 Holopix。重点验证横图、竖图、透明物件、重复图层名、嵌套组和 64 张上限；确认超过 240 秒的任务会继续等待且可在 600 秒内正常回插；取消任务应停止等待并向已提交的 ComfyUI prompt 发送取消请求。
- 在 Photoshop 中热重载当前 `dist`：没有打开文档时确认“AI编辑”“AI勾线”“AI细化”整栏都不出现；分别打开不含 ChessGo 结构的普通文档和 ChessGo PSD，确认三者都显示并默认收起。ChessGo PSD 中一级折叠栏顺序严格为“当前 PSD → AI初稿 → AI编辑 → AI勾线 → AI细化 → 生成 PSD → 运行与诊断”。先展开三栏、填写输入并调整选项，再切到需要隐藏这些页签的文档；确认页签隐藏，切回后原折叠状态、输入与选项仍完整保留。标题高度、分隔线、箭头和背景风格与其他模块一致，不出现旧 Centerline Forge 紫色皮肤。
- 展开 AI勾线，确认不再显示“Photoshop 输出”标题、“保留工作路径”、“生成工作路径”和路径名称输入；“生成描边”位于内容区最上方，没有当前会话且与活动文档匹配、来源图层仍存在的已绑定 Path JSON 时不显示“插入就绪描边”。生成成功后按钮应出现；保持新建的 `Autoline` 或选择同一文档内其他图层时按钮仍显示且可直接插入，无需切回来源图层。切到其他文档时隐藏，切回来源文档时恢复；删除来源图层后按钮应自动隐藏，插件重载后保持隐藏。描边宽度常驻；“高级选项”标题和箭头紧邻并作为一组靠左显示，标题和展开后的精细度、折角敏感度、平滑度共用同一个外框，内部没有参数分割线。四个数值框位于各自滑杆的右侧，可直接输入、与拖动值双向同步，并按各自上下限约束。确认描边宽度标题保持横排，生成的路径与 Shape 图层统一命名为 `Autoline`。在不同面板宽度下滑杆、输入框与按钮不溢出。
- 分别选择 `200×400 px`、短边恰好 `300 px` 和短边大于 `300 px` 的图层执行“生成描边”：在 ComfyUI 中确认 `LoadImage → 短边检测 → <=300 比较 → 条件缩放 → BiRefNet → GetColorToneV2 → ExtendCanvasV2`。`200×400` 应等比变为 `300×600`，短边 300 的输入保持等效 300，大图直接走原尺寸分支且不缩小；随后画布宽高各增加 `40 px`，四边各 20 px，扩展区颜色匹配原图背景主色。返回 Photoshop 后确认缩放倍率和 20 px 坐标补偿都被还原，输出 Shape 与原图层严格对齐，且为黑色、无填充、圆角描边、描边宽度正确。Shape 成功后自动清理内部工作路径，Shape 失败时仍保留可编辑路径用于恢复。
- 在已有成功结果后执行“插入就绪描边”，确认按绑定来源图层的当前位置插入最近 Path JSON，且 ComfyUI `/prompt`、图层上传和 Holopix 都没有新任务；生成期间切换到同一文档内其他图层，确认 ComfyUI 完成后状态为“描边已就绪，等待插入”而不是生成失败，按钮保持可用。执行一次超过 200 秒但少于 360 秒的长任务，确认插件持续轮询并接收最终结果，不再在 180 秒提前报错。取消按钮应位于任务进度卡右下角且可正常取消，活动文档被切换后不会写入错误文档。就绪、阶段、结果和错误信息应进入“运行与诊断”，底部不再出现独立“状态”卡片。
- 对同一未修改图层连续点击两次“生成描边”，确认两次都得到不同的上传 `run-<nonce>` 子目录、不同的 Holopix `request_nonce` 和不同 `prompt_id`；第二次 ComfyUI 历史不得再把 LoadImage、HolopixGenerateV3、BiRefNet、扩边和矢量化图像链全部列入 `execution_cached`，并应实际重新消耗完整生成时间。只有点击“插入就绪描边”才应在不新增 `/prompt` 的情况下快速创建 Shape。
- 在 Photoshop 中热重载当前 `dist`，确认工作表、批量选图、AI 工作流和 AI 棋子链四类下拉框均恢复完整的默认 Spectrum 底框与弹出层布局，同时不再出现原生 HTML 下拉框的空白横条；逐一切换选项并验证业务状态正确更新，忙碌/候选管理状态下禁用行为仍生效。
- 确认 AI 顶部工作流、物品链、候选组三行左右对齐且不显示额外标题；候选矩阵的链节点列收窄为 `60 px`，链节点与参考列保持 `4 px` 间距，参考列相较 `0.5.10` 实际左移 `24 px`，参考与候选之间的原距离不变；每行图片外的纵向留白约为此前一半，`64×64 px` 参考图和候选图不缩小；长名称和 assetCode 应正常省略且不重叠参考图。物品链显示名称与数量，生成按钮、恢复入口和候选进度仍正常显示并保持原行为。
- 用已有 4 列完整候选的链把“候选组”设为 2，不点击任何追加按钮，确认矩阵自动滚到最右端，每一行第 5、6 列都显示“待生成”，旧候选不覆盖，ComfyUI 不应出现新任务，主按钮直接显示对应未完成张数。把候选组改成 3，确认尾部同步变成 3 个待生成槽。点击主按钮后 Flux 才按物品排队，GPT Image 2 才新增对应轮数。保留一个失败槽时应优先重试而不新增下一轮；制造结果待确认时也不得自动追加。
- 确认提示词编辑器显示为默认 Spectrum `sp-textarea`，输入文字为正常字形与 `15 px` 字号，底部不再显示“重新生成选中物品 / 重新生成选中链”；修改物品 A 后切换到物品 B，再切回 A，确认尚未提交的修改仍完整保留。Flux 与 GPT Image 2 的同一物品草稿应相互隔离；右下角把手仍可在 `86–360 px` 范围内调整高度且不会突出输入框。编辑提示词后点击顶部唯一生成按钮，确认直接使用矩阵已有待生成列而不在其后插列；Flux 只为明确编辑过的物品跳过 QwenVL，GPT Image 2 在同一整链轮次中使用各物品编辑稿。上一轮运行中再次点击顶部按钮，应新增下一组并继续进入安全队列。
- 在带隐藏布局元数据的 PSD 中删除一个成员画板的 `参考图` 图层，确认该成员仍按原物品链、中文名和顺序显示在 AI 候选矩阵。Excel 仍在内存且该物品明确选图时应显示 Excel 参考图并可运行 Flux；没有任何参考图时仍可编辑提示词后追加生成，GPT Image 2 仍可包含该节点。若该画板同时没有空白智能对象，行仍保留，点击已有候选后应创建唯一目标并完成无漂移、无溢出的回填。
- 在同一画板连续点击多张候选，尤其交替点击宽高比不同的横图与竖图；确认每次都重新以完整画板为 contain 目标，候选不会一张比一张小，中心不漂移且四边不溢出。
- 生成或恢复一批候选后重载插件，确认持久化原图先恢复为可点击回填状态，随后矩阵通过两个异步直读通道补齐 ImageBlob 预览；ComfyUI `/prompt` 队列不得因历史预览新增 `ChessGo safe preview` 任务。历史预览尚未全部完成时立即开始下一轮 Flux 或 GPT Image 2 生成，确认旧预览继续加载而不暂停，新生成结束后所有缺图都会再次扫描补齐。制造一次 `/view` 暂时失败，确认请求锁释放、最多三次退避重试且不会永久漏图。重建期间切换 PSD 或工作流，确认预览不会补到错误矩阵，返回原作用域后可重新恢复。
- 真实提交一次 Flux 与一次 GPT Image 2：确认“候选图片进度”在插件内部仍有后续任务时显示“本地排队 N 项”，提交到 ComfyUI 后显示实际排队第几位，运行时依次显示 `节点 ID · 节点标题`；支持节点进度的节点应继续显示 `value/max`。窄面板中长节点标题应省略但右侧图片计数始终可见；工作流完成后标题恢复“候选图片进度”。
- 在 Photoshop 中点击候选矩阵右上角 `···`，确认展开后的操作区贴紧右侧，并与左侧表头明确分隔；逐张选择并取消选择 ready/accepted 候选，再验证“全选 / 取消全选”和已选数量。管理期间候选点击不得触发回填，生成、工作流和链条切换应禁用。确认删除后，基础槽变为“待生成”、额外尾部槽收紧；PSD 已回填图层和 ComfyUI 原图不变。重载插件并在出现待确认任务时执行历史恢复，确认已删除候选不会重新出现。
- 在 Photoshop 中重载 `0.5.7`，确认工作流和棋子链列表仍为独立控件；候选进度位于“每个物品生成”同一外框内，中灰轨道不再被 UXP 画成白色，文字与进度条、棋子链列表与上方卡片之间均保留适度间距。普通状态不显示“恢复已有候选”，出现待确认结果时该按钮仍能显示并恢复。
- 在 Photoshop 中加载当前 `dist`，确认 AI 面板最上方只有一个 `Flux / GPT Image 2` 版本列表；切换版本时矩阵布局不变，Flux 已有候选仍保留，切回 Flux 后不会显示 GPT Image 2 候选。
- 恢复或生成候选后，确认所有动态候选只使用 ImageBlob 显示；在不支持 ImageBlob 或 Object URL 显示失败的环境中，槽位必须明确显示“ImageBlob 失败”，日志不得出现 Canvas 回退，也不得创建 `<canvas>`。
- 触发一次 ImageBlob 成功或失败后导出诊断，确认 `holopix.ai` 事件包含完整 `ImageBlob 诊断`，能够区分构造器未暴露、构造失败、Object URL 失败和 `<img>` 加载失败，并记录实际 UXP、插件与 Photoshop 版本。
- 选择 GPT Image 2 后以同一链运行 1 张候选，确认日志明确显示使用 `GptImage2.json` 内置风格参考，没有读取 PSD 参考图、没有调用 ComfyUI `/upload/image`；整链只提交 1 个 `HolopixGenerateV3` 任务，输出按阅读顺序和 `assetCode` 正确拆回每一行。
- 以 `每个物品生成 2 张` 再运行一次，确认实际是整链两轮，每行得到两张独立候选；任一透明 PNG 仍可直接点击并通过既有回填链插入对应画板。
- 在 GPT Image 2 模式单击待生成或失败格，确认单格生成入口不可用；修改底部当前物品描述并点击“重新生成选中链”，确认旧候选保留，当前链每一行都在右侧追加新候选，并按完整整链重新生成。
- 制造整链只裁出部分物品，确认已经拆出的行保持可点击，缺失行单独失败可重试；重载插件后使用“恢复已有候选”，确认共享 `prompt_id` 的输出仍按 `assetCode` 回到正确行，不与 Flux 历史混合。
- 修改 `GptImage2.json` 内置 `LoadImage` 风格图或整链节点参数后重载插件，确认新模板生效；若同事机器使用不同 ComfyUI 输入目录，须确保模板中同名风格图已经存在。

- 当前最高优先级：删除 `c_cleaning1` 画板中的空白智能对象，确认矩阵仍保留 `c_cleaning1` 行且可正常生成/恢复候选；第一次点击候选后应自动出现一个命名为 `数字x数字_空白智能对象` 的嵌入式智能对象，并严格位于原画板内。继续点击同一行其他 ImageBlob 候选，确认沿用新目标替换，不重复新建，且每次都不漂移、不溢出、只回填一次。另在同画板放入两个合法命名目标，确认显示“空白智能对象不唯一”且不会生成或回填。制造创建、移动或定位失败时，确认 Photoshop 历史恢复到点击前状态，没有遗留半成品图层。
- 在旧 PSD 候选仍生成时切换到另一份可识别 PSD，确认已经提交的当前批次可在历史恢复，旧 PSD 尚未提交的节点/后续批次不再提交；切回旧 PSD 后可安全继续失败格。
- 模拟 ComfyUI 历史轮询断线或超时，确认已提交格显示“结果待确认”且不能直接点击重试；隐藏面板、切换 PSD 或重载插件后该状态仍能恢复。恢复已有候选后只填充同一 `prompt_id` 的缺失格，不覆盖已成功图片；模拟没有 `prompt_id` 时，只有显式确认放弃后才允许重试。
- 在未保存的新 PSD 上点击生成，确认插件要求先保存且 ComfyUI 没有新增付费任务；保存后再生成。取得原图或精确恢复成功后重载插件，确认候选仍为 ready 而不是 idle，不会再次进入批量生成；若暂时没有缩略图，点击“恢复已有候选”应只增强同一原图的 JS JPEG→RGBA 安全预览。
- 以 3 张候选运行一次 `2 + 1`，在第二批制造明确失败，确认前两张仍可点击、只有第三格失败；第三格重试直接复用首批 QwenVL 实际提示词。对用户编辑提示词任务制造失败时也应直接复用编辑文本。
- 用最新测试包重新生成 PSD 后，不重新打开 Excel 并热重载插件；确认链选择恢复原链条名字，矩阵第一行显示隐藏布局数据中的中文物品名（如“清洁布”）、第二行显示 assetCode，QwenVL“物件名字输入”也收到中文名。PSD 参考缩略图应通过 modal scope 内的只读取像和 JPEG data URL 显示成员画板的 `参考图` 图层，不再出现“未预览”、`only allowed from inside a modal scope` 或“不支持 ImageBlob 原始像素预览”。切换到另一份 PSD 后，链条与 AI 范围都应随当前文档变化。
- 从 PSD 单独恢复的节点运行一次真实生成，确认日志先显示只读提取 PSD 参考图，再上传并运行 QwenVL；生成前后 PSD 图层结构与历史状态不应因读取发生变化。
- 选择已有真实提示词的物品，编辑提示词后点击“重新生成选中物品”；确认旧候选保留，右侧按当前数量立即追加“排队中”槽位。整条链尚未完成时也应允许点击，新增任务等待前序任务后再生成；日志明确显示跳过参考图上传与 QwenVL，ComfyUI 历史中的本次生成直接使用修改文本。
- 重载 `0.5.0`，在候选矩阵上连续滚动鼠标滚轮，确认始终由最外层面板纵向滚动，没有矩阵内部上下抽动、卡死或抢滚轮；拖动底部横向滚动条时只发生水平位移。
- 在 2 张与 4 张候选两种宽度下左右拖动，确认标题、参考图、候选与选中蓝底同步移动；蓝底至少铺满可视区，并覆盖到最右候选之后。
- 用当前测试包拖动底部横向滚动条后，逐个点击右侧候选的中央与边缘，尤其验证“重新生成选中物品”追加到最右侧的候选；确认视觉位置与点击命中一致，回填后仍停留在相同水平位置，纵向滚轮继续只滚动最外层面板。
- 真实生成期间确认已完成候选可以立即点击插入画板，后续节点继续排队；同时调整“每个物品生成”数量，确认当前批次不变、结束后候选槽位同步到新目标。
- 只勾选链内部分节点时，确认 AI 面板与提交任务只包含实际进入 PSD 且明确选择 Excel 参考图的节点。
- 在 Photoshop 中重载 `0.5.3`，恢复现有历史候选；连续上下滚动候选矩阵、折叠再展开 AI 栏并调整面板尺寸，确认候选会在进入视口后重绘，且不再随机空白或带动上下区域卡顿。
- 继续点击候选方格的中央与边缘，确认都能稳定切换；每行候选仍分别显示、只有当前候选显示绿框，并且每次点击只回填一次。
- 重启 Comfy Desktop 中的 ComfyUI，确认进程监听 `0.0.0.0:8188`，插件可通过 `192.168.1.32:8188` 连接，另一台可信局域网设备也能打开生成的 `/view` 地址；同时确认 Windows 防火墙没有把 8188 暴露给不可信网络，并确认 `LoadImage`、`PrimitiveStringMultiline`、`StringFormat`、`AILab_QwenVL`、`PreviewAny`、`HolopixGenerate`、`ImageScale` 和 `SaveImage` 节点可用。
- 导入含内嵌图片的 Excel、选择棋子链；确认物品名称进入“物件名字输入”、参考图进入 QwenVL，“提示词结果”在生成/恢复后显示 QwenVL 返回的真实提示词，同时确认 `HolopixGenerate` 不含 `reference` 输入。
- 点击真实生成时确认不再出现二次弹窗；整张候选图可点击选中并替换对应 assetCode 画板的空白智能对象。
- 点击一个已有候选，确认只有当前选中候选显示绿框；确认回填后的智能对象完整位于原画板框内且没有溢出。
- 修改发布目录的 `Holopix.json` QwenVL 参数、Holopix 模型强度或超时后重载插件，确认新参数生效；生成比例仍固定为 `1:1`。
- 在 Photoshop 中确认仍显示 `智能对象边长 1024`、`画板间距 50`。
- 修改两个值、关闭并重新打开插件或重启 Photoshop，确认恢复上次合法输入。
- 用非 1024 边长生成 PSD，双击空白智能对象，确认内部 PSB 尺寸正确、编辑保存可回写，画板内外观仍为 `148×148 px`。
- 选择包含无关联图片提醒的棋子，确认仍生成画板和空白智能对象，且该画板没有参考图图层。
- 在同事机器运行 `release/install.cmd`，确认首次安装、同版本快速安装和网络受限时的 Codeload 回退。

## 新会话开始方式

1. 告诉 Codex 工作目录为 `D:\Scripts\UXP\PsdArchive`。
2. 要求先读 `AGENTS.md` 和 `PROJECT_STATUS.md`。
3. 说明本次要改的 UI 或 Photoshop 行为，并提供截图/诊断 ZIP（如有）。
4. 修改后要求运行 `pnpm verify`；准备给同事更新时再同步并推送 `release`。
